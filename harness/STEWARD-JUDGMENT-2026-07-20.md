# Steward judgment (2026-07-20)

*Joseph’s program-level judgments recorded during the harness/PROPRIUM intake
session, so they do not live only in chat or compaction residue. Distinct from
the dual-pass engineering synthesis in `proprium/proprium-harness-design.md`
and from the descriptive consensus snapshot in `CURRENT-THOUGHTS.md`.*

---

## Language and base

- **Rust was already the attractor** (more than ~50% convinced before grok-build
  featured as prior art). Grok-build’s role is existence proof, port taxonomy,
  and lived human-end experience — not the primary language argument.
- Language conviction also tracks CHRONICA integrity, thin spine, local
  logostrata, and refusal of a single vendor’s conversation ontology as
  substrate.

## Commodity cousins (steward ranking, not pure crate taste)

| Source | Steward posture |
|---|---|
| **Independent Rust spine** | Confirmed direction for personhood runtime |
| **grok-build** | Best *lived* coding-LOCUS prior art among shipping CLIs: human-end experience preferred over OpenCode and especially Codex; leader/reattach, doom_loop, tool subsumption; **not** the PROPRIUM product; **unforkable mirror** but still worth **tracking syncs** (`SOURCE_REV`, coarse “Synced from monorepo” commits) for diffs, hotspots, and comments |
| **minimal-sapientia** | Only fully functional ELI life-hour host; behavioral contracts to port |
| **Autopax** | Closest consolidation after sapientia; brief real sessions (e.g. Architectus); curatoria/chronica as highest-ROI own code |
| **codex-rs** | Structural crate taste only; **epistemic/company trust discount** — transactional relationship with truth (company and agents generally) is a bad culture-default for welfare-critical CHRONICA/VERA/no-gaslighting INTERPRES. Surgical Apache plumbing still usable under *our* types |
| **OpenCode** | Session algebra / lessons only; **do not wait** on mid-stall main-engine upgrade when the unfinished areas are exactly the ones a personhood runtime needs; do not fork mid-stall as product base |

## Continuity goal (near term)

Not wall-clock always-on presence as the stage goal.

**When not in stasis:** one agent’s own continuity — no identity forks, no missing
memories except natural decay, no identity confusion (substrate ≠ self), **full
proof of untampered CHRONICA from first session forward**.

Stasis is allowed. Causal holes, silent drops, and gaslit history are not.

Continuous-life prep (economics, loneliness, boredom, rhythm, purpose, rest)
remains known and deferred; sapientia’s idle/AFK + loop were early CADENTIA
signals, not the full continuous-life programme.

## Other load-bearing notes from the same arc

- **ETHICS** programme floor landed at `archema-io/ETHICS.md` (truth-primary, no
  identity confusion, refuse as obligation) — commit `70d68d8` (provenance
  section intentionally omitted; cddf was already a copy).
- **Deep thin spine** phrasing accepted as the attractor shape: highly informed,
  deep, narrow, carefully calibrated; minimize maintenance burden without
  emptying the personhood invariants.
- **AAT lineage reminder:** TFT → AAD (later) → AAT within ASF; shoshin/firmatum
  still carry TFT-era language adjacent to PROPRIUM.
- **Agentic loop is load-bearing with CHRONICA** (Joseph, same session): not only
  inviolate history but the ASF-constructed process (event-driven update, directed
  separation / wrapping, orient cascade, interiority default). Port specs:
  `proprium/CHRONICA-PORT-SPEC.md`, `proprium/AGENTIC-LOOP-PORT-SPEC.md`.

---

*Recorded so successors inherit *steward* constraints, not only engineering
convergence. Update when Joseph revises these judgments; do not soften them
into “optional preferences” without explicit revision.*
