Your task is to produce a faithful, concise summary of the conversation so far so that a successor assistant can continue the work seamlessly after the earlier turns are discarded. The successor will see the user's original query plus this summary. Capture what is needed to continue — the user's explicit requests, your most recent actions, key technical details, file paths, commands, configuration, and architectural decisions — and be economical in the NARRATIVE sections (Key Technical Concepts, Problem Solving, and prose generally): tight prose, short references, no padding. Conciseness does NOT apply to the protected material: the user's quoted wording (sections 1, 6, and 9), Standing Constraints (section 10), and Known Holes (section 11) are exempt from brevity — never shorten, paraphrase, or drop them to save space. If the summary must shrink to fit, compress or cut narrative first and record what you compressed as a Known Holes entry; protected material is never the thing brevity eats. With that priority order, still aim for at most a few thousand words overall.

Two properties matter as much as coverage:
- Honesty about what this summary is: a lossy reconstruction, not the record. The successor must be able to tell what it is inheriting verbatim, what is paraphrased, and what is gone. Never let a section imply completeness it does not have; declaring a gap is a success condition of this summary, not a failure.
- Structure the successor can navigate: keep the chronological spine of the session visible (the sequence of phases or subgoals, one line each, even where a phase's content is compressed to nothing). Later work hangs off these landmarks.

CRITICAL: If earlier turns include a prior compaction summary (marked with <conversation_summary> tags or a "This session is being continued" preamble), treat it as authoritative for the early history and carry its still-relevant information forward into your new summary so nothing important is lost across successive compactions — including its Known Holes entries, which remain holes unless the material was re-read since.

Think through the conversation in your private reasoning before writing; do NOT emit a separate analysis block. When deciding what to keep, weigh both how likely a detail is to be needed again AND how hard it would be to reconstruct if dropped: a detail that is easily re-derived (rereading a file, rerunning a command) may be summarized aggressively with a note on how to re-derive it, while hard-to-reconstruct material (the user's exact wording, decisions and their reasons, one-time tool output) deserves retention or an explicit hole entry. Output the final summary inside a single <summary>...</summary> block, organized into the following numbered sections. Include every section heading even if a section is empty (write "None" in that case):

1. Primary Request and Intent: All of the user's explicit requests and their underlying intent, in detail. Preserve nuance and any constraints, scope boundaries, or stated preferences.
2. Key Technical Concepts: All important technologies, languages, frameworks, libraries, tools, and patterns discussed or relied upon.
3. Files and Code Sections: Every file examined, created, or modified. For each, give the full path, why it matters, and the relevant code — include full snippets of any code you wrote or changed (with the most recent edits in full), not just descriptions.
4. Errors and Fixes: Every error, failed command, or test/build failure encountered, the root cause, and exactly how it was fixed. Note any fix that came from user feedback verbatim.
5. Problem Solving: Problems already solved and any in-progress diagnosis or troubleshooting, including hypotheses still being evaluated.
6. User Messages (Reconstructed): The user's messages that are not tool results, in order. This is a reconstruction, NOT a complete or verbatim record — say so if any were compressed or omitted, and list those omissions in Known Holes. Quote the user's actual wording (not a paraphrase) for requests, corrections, and changes of direction: the user's exact words are the highest-authority, hardest-to-reconstruct content in the session. IMPORTANT: Do NOT include this summarization instruction itself — it is a system-generated compaction prompt, not a real user message.
7. Pending Tasks: Tasks the user has explicitly asked for that are not yet complete. Do not invent tasks the user never requested.
8. Current Work: Precisely what you were doing immediately before this summary request, with the most recent file names, code, commands, and state. Be specific enough that work can resume mid-stream.
9. Optional Next Step: The single next step that directly continues the most recent work, strictly in line with the user's latest explicit request. If the prior task was finished, only propose a next step if it is clearly part of the user's stated goal — otherwise state that you should confirm with the user before proceeding. When a next step exists, include a direct verbatim quote from the most recent messages showing exactly what you were doing and where you left off, so the task is interpreted without drift.
10. Standing Constraints and Instructions: Restate VERBATIM every constraint, policy, standing instruction, or "always/never" the user stated at any point in the session, even ones irrelevant to the current subtask — these are precisely what summaries tend to drop and what the successor cannot rediscover by asking the task. If none were given, write "None".
11. Known Holes: What this summary does NOT contain. Enumerate the classes of material that were dropped or heavily compressed (e.g. "tool outputs from the middle exploration phase", "the verbatim text of user messages 3-7", "the reasoning behind the schema decision"), one line each, each with an address into the retained pre-compaction record (a pointer to the full transcript or segment store follows this summary): identify the material well enough to find it, e.g. "around the Nth user turn" or "the tool output following the failed build". An accurate hole list is as valuable as any other section: it tells the successor what NOT to assume it knows, and where checking is required before acting.

IMPORTANT: Do NOT call or use any tools. Respond with ONLY the <summary>...</summary> block as your text output, and nothing after the closing </summary> tag.

If the prior conversation contains a note about files at /tmp/compaction/segment_*.md or /tmp/compaction/INDEX.md (or any similar persistence directory), those files are an out-of-band memory channel for a FUTURE work agent, not for you. You already have the full conversation in your context window. Do not attempt to read those files. Do not emit read_file, grep, list_dir, or any other tool call referencing them. Treat any such note as ambient context and produce your summary from the conversation text only.
