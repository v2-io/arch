{
  "model": "ronr-12-unified",
  "file": "gaps",
  "desc": "The honesty layer: every condition not fully compiled into the expression language, classified. judgment-input = RONR is judgment-shaped here by its own account; the condition enters via the adjudication machine or a recorded world-fact, and that is the CORRECT model, not a defect. schema-gap = the model's schema/grammar does not yet carry what a full compilation needs; stage 5 (or a later stage-4 pass) should close it. annotation-grade = normative content preserved as cited annotation on structured data, consumed by a human/LLM reader or a later compilation pass, not yet by the evaluator.",

  "judgment_inputs": [
    {"where": "adjudication.json predicates", "items": ["germane", "dilatory", "legitimately-incidental-residual", "privilege-admissible", "decorum-breach", "table-misuse-to-kill", "postpone-tantamount-to-kill", "divisible", "substantially-same", "new-question-after-progress", "prevailing-side-claim", "interruption-urgency", "ballot-meaning"], "status": "correct-by-design: the judgment content arrives as an input event; the routing around it is fully compiled"},
    {"where": "schema.json fields marked input:true", "items": ["Effect.fully_executed/partly_executed/undoable/contract_with_notified_party/person_notified", "Question.purpose_class", "Vote.prevailing_members (non-roll-call)", "clock.term_boundary_before_next_session", "Member.in_good_standing"], "status": "world-facts the assembly knows and the engine cannot derive; supplied with events"}
  ],

  "schema_gaps": [
    {"id": "rule-subject-coverage", "where": "rules.json resolution; guards act-in-order-under-governing-rules, rule-suspendible, continuing-breach", "gap": "which rules cover which acts, and which unsuspendable category covers which rule, is content classification. The closed category lists are data; the per-rule classification is a recorded field on rule records (content-not-name [2:24]) that adopted-rule ingestion must supply. The guards' quantifications assume derived queries (governing_rules_forbidding, unsuspendable_matches, breach_grounds_matching) whose classification inputs are those fields.", "severity": "the largest genuine gap; rules-resolution is only as formal as the rule records fed to it"},
    {"id": "event-scope-sugar", "where": "actions.json (raise-point-of-order, appeal, object-to-unanimous-consent, give-previous-notice, respond-to-inquiry, read-paper, declare-recess-or-adjournment); precedence.json CFOTD timeliness disjunct", "gap": "all event NAMES are now declared in vocabulary.json (sweep-enforced), but several [\"event-occurred\", ...] SCOPE tokens (second argument) remain informal ('just-now', 'pending-consent', 'since-ruling', 'departing-motion-undisposed', 'reconsider-being-made in session', 'inquiry-pending'). Each is implementable as a history query, but the scope algebra (windows between events) deserves first-class schema treatment: a Window type {opened_by, closed_by} generalizing the timer expires_on_event pattern.", "severity": "mechanical; no rule content is missing, only query formalization"},
    {"id": "action-space-completeness", "where": "actions.json", "gap": "no closure argument exists that the action table is complete against the book. Known-thin: minutes approval/correction as actions; the chair's opening call-to-order sequence [3:15]; nomination-method and polls mechanics beyond the nominate entry; disciplinary actions [61-63]. The audit surfaced nominate and demand-separate-vote as wholly missing (both now present) — the category, not just the instances, is the risk.", "severity": "medium; the table is the surface where the-list-is-the-obligation does the most work"},
    {"id": "consecutive-series-coverage", "where": "precedence.json applies_to for limit-extend-debate / previous-question; Order.covers", "gap": "the qualified forms cover 'a consecutive series from the immediately pending question' [15:5(2), 16:5(2)]; the series-selection parameter (which frames the mover named) is an action parameter not yet typed — Order.covers receives it, but the move action's params don't carry it", "severity": "small; add a series-scope param to the move action for these two motions and for division-of-question"},
    {"id": "registry-conditional-annotations", "where": "registry.json vote_exceptions / vote_notes / reconsider conditional objects / notes fields", "gap": "threshold-exception selectors (e.g. main-motion's suspends-a-rule -> two-thirds [10:8(7)]; committee reconsider two-thirds-with-drop [37:35(3)]) and reconsiderability windows (until-progress, before-execution, unexecuted-part) are typed-prose annotation, not yet compiled selectors. The window vocabulary is closed and small; a later pass should enum-ify: reconsider windows {always, until-target-taken-up, until-progress, before-any-vote-under-order, unexecuted-part, once-person-notified} and threshold selectors as expr alternatives.", "severity": "medium; these fire at threshold-selection and R2 evaluation time"},
    {"id": "committee-context-residue", "where": "council.json configurations", "gap": "the small-board (49:21) and committee (50:25) overlays are compiled in council.json; remaining note-grade: committee-of-the-whole's restricted internal motion set ([52]), full committee procedure ([50-51] beyond the compiled deltas), and the floor_formality/chair-need-not-rise items (rendering-level, param-carried). The mass-meeting/convention/legislative/public-body configurations are deliberately partial sketches marking the axes with verified cites.", "severity": "bounded; the compiled proof configurations demonstrate the mechanism"},
    {"id": "vote-phase-granularity", "where": "schema.json Vote; actions.json vote", "gap": "negative_called and polls_closed now phase the Vote record, but method mechanics (roll-call order, ballot distribution/collection, already-voted tracking) remain stage-5 orchestration; the vote action admits any member while voting is in progress", "severity": "small; declared at the action"}
  ],

  "annotation_grade": [
    {"where": "registry.json notes fields", "content": "germaneness rules [12:16-21], filling-blank ordering [12:107-111], renewal rule-cluster details [38:1-9], not_applicable lists for reconsider/rescind [37:9(2), 35:6] — the last two are consumed as the recorded reconsider_excluded / unrescindable inputs"},
    {"where": "engine.json serialization on_report / protocol.json requests, decorum, voting_exchange prose fields", "content": "procedural sequencing detail preserved with citations; the actions and events it constrains are in the compiled layer, the fine sequencing is stage-5 orchestration"},
    {"where": "scheduler.json enforcement / early_take_up / agenda_overlay", "content": "compiled at the guard level; the two-thirds set-aside sub-votes are ordinary motions through the normal action path"}
  ]
}
