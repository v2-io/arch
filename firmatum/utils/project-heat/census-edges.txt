# census-edges.txt — typed relations between census projects.
# Blank lines and # comments ignored. Three kinds of line:
#
#   inverse: REL <-> REL          declares an inversion pair
#   alias:   NAME -> REL          shorthand that expands to a relation
#   FROM REL TO [INV]             an edge, reading as a sentence; FROM/TO
#                                 comma-lists (globs ok), REL/INV comma-lists
#                                 pair positionally with FROM when FROM is a
#                                 list; INV omitted => derived from the
#                                 inverse table; '-' => none.
#
# Either side may declare (influenced-by on the successor is as valid as
# influences on the predecessor); readers collapse both-side declarations
# into one directional edge set.

# --- inversions (Joseph, 2026-09-01) ---
inverse: predecessor-of <-> successor-of
inverse: depends-on-critically <-> critically-needed-by
inverse: informed <-> was-influenced-by
inverse: influences <-> is-influenced-by

# --- aliases (Joseph, 2026-09-01) ---
alias: requires -> depends-on-critically
alias: informs -> influences
alias: became -> predecessor-of

# --- edges ---
# From the 2026-09-01 session (Joseph, in dialog): chiridion influenced how
# he thinks about udon stores and verisectorium; so did rowan, "quite
# strongly."
chiridion influences arch/udon,arch/verisectorium
rowan influences arch/udon,arch/verisectorium
