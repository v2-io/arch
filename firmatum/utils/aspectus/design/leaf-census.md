# Leaf census

Leftover *siblings* the line budget could not list:

```
└── [+4: 3 .txt, 1 dir]
```

The `+` marks “not listed,” distinct from an unexpanded directory’s dir census (`[2: 1 dir, 1 .txt]` on that directory’s own line).
