# build-order

Order of work for the first snapshot. From `FEATURE-PIPELINE.md`: a feature does not jump the pipeline because it is interesting. Extra columns before this picture is honest are a worse `eza -T`.

*History of the first crate snapshot (absorb + line-share). Not Part I order.*

Part I is Help → Config → Two-level. Two-level **lists** `.git` as a child. Furniture absorb is a later row. Config is a caller stack and comes *before* Two-level.
