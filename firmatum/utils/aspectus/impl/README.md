# impl/

Optional worklogs for Part I stories. Filename = slug, same identity as the Feature column (`form-allocator`, not a path).

Create a file when there is a DAG, a blocker, a claim, or a finish note. `Todo: —` on the outline until then.

Not present-truth — that is [`../design/`](../design/) or the Foundation pointer.
