# labels — finish note

*Landed. Source: `src/furniture.rs` (kinds on rules), rendered by `src/n_level.rs::state_marks`. Tests: `tests/labels.rs`.*

`[has: …]` (rendered `[kind: …]` until the 2026-08-14 rename) on a directory line, sorted, deduplicated, fed by hide rows and mark rows (`Cargo.toml` → `rust` listed-and-claimed). Empty set prints nothing. Files carry no kind on their own lines. Claims survive at the depth cutoff (recognition is name-based, from the free readdir enumeration). Multi-kind rules where the name honestly supports it; no guesses — see `design/labels.md` for the `target/` reasoning.
