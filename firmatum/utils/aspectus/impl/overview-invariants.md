# overview-invariants — finish note

*Landed. Source: `src/overview.rs`. Tests: `tests/overview.rs` (real binary) plus `overview::tests` for the UTC stamp.*

Header line: `absolute/path/  YYYY-MM-DDTHH:MM:SSZ`. Path from `std::path::absolute` (logical, not realpath). Stamp is UTC second resolution, when this print ran.
