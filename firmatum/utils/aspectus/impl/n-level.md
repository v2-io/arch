# n-level — finish note

*Landed. Source: `src/n_level.rs`. Tests: `tests/n_level.rs`.*

`--depth N` is generations below the root. Built-in default **2**. `0` is unbounded. Config key `depth` on the caller stack. `--depth 1` is children only (the first shipped picture).
