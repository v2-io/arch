# Hallway tests — 2026-08-22 (v0.1.15, after grid-cleanup steps 1–5) — coordinator's routing

*Two unprimed end-user reads of the current look, launched the minute v0.1.15 was installed, neither shown the other or the prior audits: **Sonnet 5** ([`hallway-sonnet-2026-08-22.md`](hallway-sonnet-2026-08-22.md), 9 tool uses, ~80 s) and **Grok 4.6** ([`hallway-grok-2026-08-22.md`](hallway-grok-2026-08-22.md), captured stdout/stderr to files and read them whole — the agent-shaped encounter; it honestly declared the always-on estate brief as priming). Both verbatim, untouched. This file is the routing: what converges (independent priors ⇒ signal), what each found alone, and where each item goes. Convergence mark: **◆◆** both readers, **◆** one.*

## Convergent findings (both substrates, unprompted)

| # | finding | route |
|---|---|---|
| ◆◆ 1 | **The `≈ ~ ≥` marks don't stick** — Sonnet: re-looked up on every output, "needs a cheat sheet physically present"; Grok: *"the one that looks most like 'about' is the one that claims exactness… I had to correct this more than once"*; used them "as flavor instead of as claims." Now the **fourth** independent arrival (Grok 08-14, Sonnet, Grok today, the close audit). | **Joseph — a decided glyph is in question.** Proposal for ratification: *exact-grouped carries no mark* (`61.2K` bare — grouping is the number's own format), `~` estimate, `≥` floor — so a mark means "not exact," which is what every reader already assumes. Recorded in `design/grid-cleanup.md` §count cell notes. |
| ◆◆ 2 | **Column sets shift between looks** (a `perms`/`mtime`/`bytes` column present on one tree, absent on the next) with no per-run signal; Sonnet names it the one *usability risk* ("silent misreads"); Grok: "the tool grows extra clocks when git is present." | Design: `design/defaults.md` / `design/grid-cleanup.md` Open — quiet columns appear by law; candidates: a stable column *order* always; a visible mark on headings of quiet-spoken columns; or reserving far-right width for the `[layout]` list regardless of speakers. Joseph's call on the trade (width vs. stability). |
| ◆◆ 3 | **`⊘` is misread** — Sonnet: "denied"; Grok: "empty/blocked… 'this is not for you'"; both needed the help table. Unlabeled, unheaded column. | Design: git-status pack is decided, but the glyph's first-contact reading is now measured twice as wrong-direction. Route to the pack's cold-read test (`design/grid-cleanup.md` §packs, test 1) — it failed it. Candidates: heading the far-left column; or a different ignored glyph. Joseph. |
| ◆◆ 4 | **`[has: …]` reads as a flat bag / "a rumor with a number"** — neither could resolve what `agents` meant from the look; `agents ≈0f`, `trash ≈0f` distrusted; `archive ≥1186f` at `~/src` root "I still don't know where." | **The cultivated-header brainstorm** (`design/wanderings-2026-08-22.md` §3; lattice-2 §Claims). Primary demand evidence for it. |
| ◆◆ 5 | **Empty directories are silence** — Sonnet didn't flag; Grok: *"silence is the one confession that doesn't confess"* (`glimmer/`, `INGEST/`, `agentic-systems/` with a leftover `.git`). Third arrival (usability pass item 4). | Design open (`empty-dir` row, lattice-2 ⇥). Joseph: `[empty]` or a shape — not `∅`. |
| ◆◆ 6 | **`M├──` / far-left cell cramped and unheaded** — Sonnet read `M├──` as a compound glyph; Grok wanted a heading for the git-status column. | `design/grid-cleanup.md` §Step 5 note: a separating cell between the block and the tree prefix (one cell per row), and/or a one-glyph heading. Joseph's layout call. |
| ◆◆ 7 | **`aspectus config` is the real documentation**; both would run it right after help; Grok: help "is a monograph… the first picture is how I learn the tool." | `design/help-verb.md` Open: lead with a one-screen flag/recipe table, essay after; examples should lead with the orientation recipe both readers arrived at: `--depth 1 PATH`, then `--lines 0 --depth 1 PATH`, then enter. (Usability pass item 9 said the same from the other side.) |
| ◆◆ 8 | **Heat vs age two-clock confusion at multi-repo roots** — both compared heat across repos despite help. | Known open (`design/heat.md` §Open, cross-repo scale). Evidence strengthened. |

## Found by one reader

| # | finding | route |
|---|---|---|
| ◆ Grok 9 | **Directory mass swings across looks** — the same `asf/` read `≥ 1.0M`, `~ 2.4M` (as part of arch), `~ 679.1K`, `~ 1.1M` under different `--lines`/`--depth` in one session: *"directory line-totals are not a fact I can carry to the next look"* — the strongest distrust it developed. | Design: `design/mass.md` — the read-budget *share* makes the deep estimate walk-relative by construction; the `~` confesses it but the reader still carries the first number. Candidates: size-based estimate always (budget-independent, marked `~`), or exact counts via the cache row. **Joseph** — it's the trust of the headline number. |
| ◆ Grok 10 | **Help's "default 2" vs this machine's `depth = 3`** — "the first place I was actively misled, by the binary's own prose about itself." | Already decided, unbuilt: the **config-drift header** (`design/overview-invariants.md` §Config drift). This is its evidence; promote on the pipeline. |
| ◆ Grok 11 | **`--walk 30 ~/src`** spent the whole bound inside one unlucky early directory and expanded it fully — "the remainder confesses; the emphasis still lies." | Known open: cross-level spend order (`design/walk-bound.md`). Second arrival with a specimen. |
| ◆ Grok 12 | **Exit 0 when one of several paths is missing** — would have hidden the miss behind `$?`. | `design/focus.md` §Multiple paths — the exit-vocabulary open (same family as the partial-look exit code). Joseph. |
| ◆ Grok 13 | **`--inspect git` on a non-repo is silent** — "the flag did not explain that it had found nothing to inspect." | Refusal/teaching law (`norm-refusal-names-class`): a no-op ask deserves a stderr line. Small; decided by existing law — queue as a fix. |
| ◆ Grok 14 | **A file as PATH renders a different grammar** (`53.  𝓁  heat 0.00 · 3.0w ago`) — the unit glyph shows here and nowhere else. | `design/grid-cleanup.md` count cell: the file-root header line should use the same cell under the same rule (heading names unit → blank `u`; no heading → `𝓁`). Consistent already by law; the *heat* word and layout differ. Queue as a fix. |
| ◆ Grok 15 | **80 lines of `asf` shows asf's root markdown, not asf** — "looking at the repo from the parent is more like seeing the place." | The root-files-vs-directories budget tension (hallway 08-14, routed to Joseph then). Third arrival; important-files + recency spend the budget on instrumentation files. |
| ◆ Grok 16 | **`dirty<3>` → `dirty<4>` moved** between looks without writes to `arch`. | Likely true (this session was committing into arch); worth one check that the porcelain call is per-repo-root, not per-look-root. Queue as a check. |
| ◆ Sonnet 17 | **Heat half-life assumed days** (it's commits). | `aspectus config` should print the unit: `heat.half-life = 7 commits`. Data-carries-units law (eyes concern 9). Queue as a fix. |
| ◆ Sonnet 18 | **Census ambiguity**: does `≈310f` include the suffix buckets after it? Couldn't resolve; flagged honestly as unresolved. | The subgroup-subject form (Joseph's next design item) — this is the grammar question stated by a reader. |
| ◆ Sonnet 19 | **Wants "since last look."** | `design/last-look.md` — demand evidence. |
| ◆ Sonnet 20 | **Sha columns discovered only via JSON** ("oh — there's more here than I thought," the good kind). | None needed; notes that `config`'s inventory is where an agent learns the asks. |
| ◆ Grok 21 | **Census noise**: `2×5`, `other×11`, `relata` as a suffix. | Usability pass item 7 (numeric-suffix junk); `other` needs a gloss. Queue as a fix with the subgroup form. |
| ◆ Grok 22 | **stderr footer is invisible when stdout is redirected alone**; dim/color doesn't travel through pipes, so `⊘`'s dim overlay is gone for agents. | By design (Joseph moved the footer to stderr 2026-08-22); the color note confirms the glyph-is-the-carrier law was right. |

## What both trusted (keep)

Remainder lines `[+ …]` ("the honest ellipsis — I trusted this before I trusted the tool"); the stamp; `[cycle]`, `->`, `[walk bound]` as marks; the git facet; file line counts; recency-sort teaching itself in one row (Grok's own capture dir at `0m ago`); quiet facts pulling the eye exactly when weird (`root`, `700`, a 20 MB svg); `--lines 0 --depth 1` as the complete child list; JSON `truncated` as a completeness claim; show-all vs default as a pair; the `M` glyph confirming a known dirty file.

## Disposition

Decided-by-existing-law fixes, queued for the next code slice (no design needed): 13, 14, 16, 17. Design opens strengthened, Joseph's: 1, 2, 3, 5, 6, 9, 12 (+ the known 8, 11, 15). Evidence into the header/subgroup brainstorm: 4, 18, 21. Promote on the pipeline: config-drift header (10). Help shape (7): queue after the header work so examples teach the final grammar once.
