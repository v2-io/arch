# TODO — the main integration queue

*Renamed from six "residue" registers on 2026-08-06: calling the remaining work "residue" framed the main deliverable as leftovers, and that framing licensed a false completion claim. This is **the-main-thing-we-need-to-do**: ~120 items of note-content not yet carried by segments, each routed to the outline row it should feed. An item is discharged only by landing in a segment or by express dismissal with a reason. Sources named by items stay in INFLUX until their items are discharged ([[integration-metabolism]]: a register locates debt; it does not pay it). Entry-ids (R…, A…, N…, V…, R-T…) are stable and cited from segment Working Notes — do not renumber.*


---

## From `INFLUX-DISPATCH-2026-08-05.md`

# INFLUX dispatch & residue register — 2026-08-05

*Fresh-eyes pass over the twelve drafted segments and the overlapping INFLUX notes (segments read first, then the notes). This file is the durable carrier for **residue**: real content in dispatched or still-queued notes that no drafted segment carries. Each row names the OUTLINE row it is owed to, so it can be discharged when that row drafts.*

*This is a present-truth surface about **what is owed**, not a history layer. Retire a row when its content lands in a segment body (or is expressly rejected). The dispatch table below is the history half and is append-only.*

**Verification marks.** ✔ = verified first-hand during this pass (2026-08-05, commands re-run against live trees). (scout) = reported by an INFLUX synthesis, not independently checked here.

---

## Dispatch record

| Note | Destination | Why |
|---|---|---|
| `udon-theory/*.udon` (5 segments + canon outline) · `udon-theory/theory-format.md` | `.archive/udon-theory/` | ✔ byte-identical to the live originals at `~/src/MOVED/udon/v2/theory/{src/,UDON-THEORY-canon.outline.udon,FORMAT.md}` (diffed, modulo provenance banner + one blank line). Copies of live authority — archive-shaped by construction. **Live corpus is the citable prior landing.** Caveat carried: that tree sits under `MOVED/` and the field report flags udon paths as unstable — the archived copies are the stable record if the live path moves. |
| `primary/cross-corpus-generalization-2026-07-23.md` | `.archive/primary/` | ✔ byte-identical to the live original `~/src/arch/notes/outline-segments-generalization-2026-07-23.md` (twin also under `MOVED/udon/.../refine-more/`). Copy, never authority here. Residue below cites the **live** path + §Part. |
| `synthesis/scatter-inventory-2026-08-05.md` | `.integrated/synthesis/` | Gather map. Its instance roster is superseded by `INFLUX/instances/deployments.md` (still live) and its claim-landing table by the now-archived udon-theory copies; its unique items are registered as residue R19–R20 below. |
| `synthesis/live-state-field-reports-2026-08-05.md` | **stays in INFLUX** | Highest residue density in the set (R1–R11). Ten of its findings have no segment home; several of its ✔ numbers are load-bearing for undrafted ch. 3 / 8 / 11 / 14 rows. Premature to dispatch. |
| `instances/deployments.md` | **stays in INFLUX** | Live-useful roster, still being consumed. Carries the cousin-store lineage, paper-project judgments, build projections, and the deliberate "not a verisectorium" list (F) — content routing to a half-dozen undrafted rows. |

**Untouched (honest incompleteness).** This pass read only the overlap set the brief named. `asf/` (11 files), `udon-analysis/` (5), `tribunal/` (14), `vera/` (22), `instances/` beyond `deployments.md` (20), `build/`, `methodology/` were **not** adjudicated. INFLUX is roughly 1/9 emptied by file count; no claim is made about the rest.

---

## Residue register

### From `synthesis/live-state-field-reports-2026-08-05.md` (stays in INFLUX)

| # | Residue | Owed to |
|---|---|---|
| **R1** | ✔ **The denormalization worry is empirically dead.** `stage` is duplicated between OUTLINE rows and segment frontmatter across asf; re-checked on 01-aat-core this pass: **168 rows compared, 0 mismatches** (1 outline row with no file). The logical-model note calls this denorm "a Codd anomaly"; measured drift is zero. The *negative* finding is as valuable as the positive one and no segment carries it. | ch. 14 [[corpus-instruments]] / ch. 12; a counterweight wherever normalization is argued from theory |
| **R2** | ✔ **What actually rots, ranked** — provenance **paths** (106 failures), **absence handling** (unexercised GAP laws, rows-without-files, unknown-stage buckets), and **verification backlog**. What does *not* rot: generated views (every one checked consistent with sources) and stage denorm (R1). [[provenance-rot-specimen]] carries only the first leg. | ch. 14 gap (what a standard health report contains); ch. 2 [[absence-as-structure]] |
| **R3** | ✔ **Taught blindness, legislated.** `asf/bin/lint-outline` reports 0 orphans on 02-tst-core because it exempts `old-*` at three sites — and does so **per FORMAT**, i.e. the blindness is *declared law*, not a bug. Sitting under it: 29 live segments vs **43** `old-tst-*` files carrying three prior identity regimes (FP-numbers → T-labels → slugs, all physically present). The sharpest available specimen for the taught-blindness hazard the ch. 14 row already names. | ch. 14 [[corpus-instruments]] |  **DISCHARGED 2026-08-06 → [[corpus-instruments]]**
| **R4** | ✔ **A law written and never exercised.** vivarium wrote the estate's strongest GAP law and has **0 `--GAP--` rows over 115 segments**. asf 03/04 also have **0** (vs 01: 9, 02: 5) but express absence as rows-with-no-file — 20 dangling slugs + 5 real dependency breaks (scout) — so the immature absence dialect *silently breaks* `depends:`. This is [[ladder-never-fired]]'s shape a second time, on a different mechanism: **the estate writes absence law it does not run.** Strong candidate for a sibling obs segment. | ch. 2 [[absence-as-structure]]; possible new obs row |
| **R5** | ✔ **Same schema, opposite discipline.** neurips/refs: 188 entries / 13 verification dirs (**7%**). logos/refs: 97 entries / 70 (**72%**). Identical layout and machinery; the difference is the consuming deployment's stakes (logos gates anonymization before submission, neurips warns). This is the cleanest measured specimen for "gate-vs-warn belongs to the deployment, not the schema." | ch. 8 [[verification-provenance]]; ch. 11 [[instance-profiles]] |
| **R6** | ✔ **Retirement backlog has a number.** `def-chronica` is **63% Working Notes** (67 of 106 lines) — and it is among the *most promoted* AAT segments. The pattern the field report names: promotion without sweeping, because Gate 4 never fires. [[ladder-never-fired]] carries the ladder half; this is the cost half, and it is the concrete case for "retirement backlog is where steady-state is won or lost." | ch. 3 [[working-note-lifecycle]] |
| **R7** | **Live cross-pollination gap.** vivarium's FORMAT open-questions ask *in writing* for nearly the exact fields udon-needs already ships (`sources:`, a mechanism field) — and nobody told vivarium. Meanwhile the two **disagree on principle**: vivarium's law says qualifier-in-frontmatter is the failure mode ("the claim travels and the qualifier does not"); udon-needs made the qualifier machine-readable in frontmatter. Both halves matter: the estate has no channel by which one instance's solved problem reaches another's open question. (scout on the FORMAT text; the udon-needs schema is ✔.) | ch. 1 inner-section gap (section-vs-frontmatter epistemics); ch. 2 estate-layer gap |
| **R8** | **Four outline-table dialects**: 01/02 six-column; 03/04 five-column; vivarium five-column + importance-tier, no §; udon-needs wikilink refs with ragged widths. "Any shared lint/build must eat these as reality." [[view-genres]] surveys *genres*; nothing carries dialect variance *within* a genre — which is the harder problem for a generator. | ch. 11 [[generator]]; ch. 12 gap |
| **R9** | **Genres no roster listed**, missing from [[view-genres]]: `residue.md` (a **do-not-resurrect named-absence store** — a negative view, arguably its own genre), relata's `quarantine/` + `deny-list.yml` + `calibrations/` (rejection and calibration machinery nothing else has), vivarium `.super-archive/.../core/OUTLINE.md` (dead prior generation). Also the recorded false positive: `~/src/_gems/devex/OUTLINE.md` is **not** an instance — keep so it is not rediscovered as missing. | [[view-genres]] (append rows when next edited); ch. 8 [[evidence-ledgers]] for quarantine |
| **R10** | ✔ **comproprium is half-outside its own store**: ~**51** harvest segments under `INGEST/` unintegrated (≈half the corpus), and lopsided within it — 69 quotations vs 8 probes / 4 principles / 1 practice. Plus one undeclared type-vocab drift (`:type pattern`). A live instance of exactly this project's own influx problem, at scale, with a measured backlog. | ch. 4 [[influx-queues]] / [[integration-metabolism]]; [[type-vocabulary-locality]] (enforcement of the local vocabulary) |
| **R11** | **relata is not a peer cousin but the generalized successor** — 4th generation of a documented lineage (neurips/refs → asf/terminology → logos/refs → relata), 2,277 entries + 219 verification dirs, external data tree, and the only instance with quarantine / deny-list / calibrations. [[substrate-independence]] cites its external tree; nothing carries the *lineage* or that the most-evolved instance is the one that left the code repo. | ch. 11 [[instance-profiles]]; ch. 8 [[confidence-calibration]] |

### From `primary/cross-corpus-generalization-2026-07-23.md` (archived — cite the live original `~/src/arch/notes/outline-segments-generalization-2026-07-23.md`)

| # | Residue | Owed to |
|---|---|---|
| **R12** | **The autopax/Archema retrofit evidence (§Part 2, from doc-store review §1.4)** — the most consequential unhomed item in the whole set. Phases 0–2 of the framework graft landed **within three days** into subsystems that were new or thin; Phase 3 — CHRONICA and TRACTUS as resources, blocking item *"verify BLAKE3 hash chain compatibility"* — **was never started**. Seven months later `CHRONICA-PORT-SPEC.md` treats autopax's hand-rolled log as source material for an independent Rust spine, with the framework absent. The honest confound is recorded in the source (attention moved to ASF, per the harness status note). What survives the confound: **the graft covered the thin subsystems and never reached the invariant-bearing one, and when that subsystem was finally addressed the framework was not used.** Generalization: *a schema layer that cannot absorb the integrity-critical subsystem has located the case that will outlive it; sequencing the hardest subsystem first is diagnostic either way.* This is direct, costly, in-estate evidence about the exact thing ch. 11 proposes to build, and no segment touches it. | ch. 11 [[generator]] and its migration gap — arguably its own segment |  **DISCHARGED 2026-08-06 → [[retrofit-specimen]]**
| **R13** | **Two candidate independent-corroboration legs, named and unexamined.** Every drafted `form` segment declares its ceiling gated on outside-authorship evidence and states none exists. The source names two: (a) Joseph, 2026-07-23 — roughly a dozen high-quality **open-source harnesses** that independently took up ideas prototyped here, *"the only material mentioned so far with different authorship"*; (b) the 23-repo / 6,201-migration survey and 20+ agent hallway-testing challenges (doc-store review §10). Both have failure modes genuinely different from the estate's. **This is a live strengthen-action the segments do not know about.** | [[turnover-solution]], [[substrate-independence]], [[collision-staleness-detection]] — all three ceiling statements |
| **R14** | **The separation test**: *two labels earn separation where they route to **different repairs*** (TST-extension-memo §2). The source suggests this test "might be the more portable artifact than any particular axis set," since it is how each domain independently discovered which axes it needed — with three worked instances of axis-collapse and its repair (vivarium's `status`/`stage` merge where `verified` meant both "checked against code" and "feels solid"; support-kind embedded as rungs on the strength ladder, disproved by its own division; T1–T5 genre codes dissolved as carrying no distinct work). The lead session's own fork-friction report ("Strength & grounds smears evidence-kind and strength") is a **fourth** instance of the same collapse, arrived at independently — the test would have caught it. | ch. 8 [[epistemic-axes]] / [[strength-ladders]]; ch. 1 inner-section gap |
| **R15** | **The convergent-lock rule, stated**: a lock is armed only by ≥2 support-kinds with *independent failure modes*; within-kind agreement raises strength **without** arming it — with a specimen where a chapter claiming three-way convergence turned out to rest on one leg because all three were facets of one author's work. Every drafted segment *practises* this discipline; none *states* it, so it is not citable and a future agent must rediscover it. | ch. 8 [[evidence-ledgers]] |
| **R16** | **Layout and visibility are two questions, not one** (§Part 3, Joseph-endorsed deconfliction). *Layout* — same file / sibling / child table — follows from whether the part changes in the same atomic changeset as the body. *Rules and visibility* — canon / non-canon-but-live / historical / derived — is independent, and is the one the build should read a **declaration** for. Conclusion: **Working Notes being in-file is not the defect; the hardcoding of their removal is.** [[coupling-confounding]] covers the co-change confounder but not this split or its conclusion. Companion: two build operations that feel alike are different in the schema — selecting *which records* appear (filter over the table by type) vs *which parts of a record* appear (WN stripped — projection over parts); both currently hardcoded. | ch. 1 [[atom-as-cluster]] / [[write-semantics-declaration]]; ch. 2 [[view-edge-metadata]]; ch. 12 |
| **R17** | **Gate 4 is already a typed three-valued act that leaves no trace.** FORMAT requires every Working Note be **resolved** / **deferred** / **promoted**, then deleted — a decided, typed disposition — but nothing records it, so *the note and the transport event disappear together*, and an undispositioned disappearance is undetectable. Emitting the disposition as an event would make the crossing observable. With it, the reframe that makes it cheap: **the indirection selective transport needs may be temporal rather than spatial — a gate as an observable crossing rather than a wall.** Filesystem distance enforces itself by friction but taxes the comprehension and concurrency proximity was serving; procedural indirection needs tooling to have force (`bin/term` writing decision events rather than trusting authors is the local precedent). Grain caveat: absorption is gradual and diffuse while events are discrete — the disposition is the plausible grain; per-fragment tracking is costume. | ch. 4 [[observable-crossings]] / [[gates-need-destinations]] — close to a drafting brief |
| **R18** | **Strata mixing: rectified vs symmetric** (§Part 5, least-settled). Two exchange processes share the same interfaces — *rectified selective transport* (material ascending toward canon: directional, gated, lossy by design) and *symmetric unselective contamination* (changelog voice into Working Notes, diff voice into canon: bidirectional, ungated). Joseph's correction is load-bearing: **mixing is the mechanism the system runs on, not the pathology.** The Kelvin–Helmholtz picture fits only the second (turbulent entrainment is symmetric, so it cannot model directional promotion); what it offers is that **shear — a difference in *rate*, not in kind — is the driver, and stratification stabilizes**, predicting that parts changing at the same rate can share a file regardless of how different their canonicity is (which is the body/Working-Notes case). Explicitly *not* carrying: momentum as conserved quantity, growth rates, Richardson numbers — costume. | ch. 4 [[layer-speeds]]; ch. 7 gap (which properties carry the effect) |
| **R19** | **Two smaller carried items.** (a) The **harness's claim-atom is unknown** and has no OUTLINE row at all — asf's is a claim, vivarium's a nomos, udon-needs' a demand, logos' a paper-argument; the untested guess is ADR-shaped, with autopax's ADR system as local prior art (status state machine, `supersedes`/`superseded_by`, typed `blocked_by`/`needed_for` edges, immutability once decided). Joseph's read was pending as of 2026-07-23. (b) **Events are forced by two independent constraints** — an independent write clock *and* being where gates are recorded — with the portable heuristic attached: *structures forced by two independent constraints have usually been more durable than ones justified by a single argument.* | (a) ch. 10 parking lot / possible new row; (b) ch. 1 [[atom-as-cluster]], ch. 13 |
| **R20** | **A second specimen for collision, from the failure side** (doc-store review §11, via §Part 7): **append-only supersession cannot signal deadness** — autopax needed a *banner in the body* because frontmatter supersession alone did not stop readers. [[collision-staleness-detection]] rests on the vivarium eleven-day rename specimen alone; this is a second, differently-sited case of the same asymmetry, arrived at from the negative direction. Also unifying under the same move: the `empirica:` contract (a claim citing an experiment with no recorded run is a **truth-status defect**, not a broken link), `OUTLINE-accepted.md` reporting stale rows, and `lint-outline`'s stage-versus-file check — all one asymmetry seen from different sides. | [[collision-staleness-detection]] (specimen + the unification reading) |

### From `.integrated/synthesis/scatter-inventory-2026-08-05.md`

| # | Residue | Owed to |
|---|---|---|
| **R21** | **Gate-or-warn is a deployment property, not a field property** (doc-store §12.2, Joseph as steward 2026-07-23): it tracks the consuming deployment's **stakes and reversibility**. Connects to the enforcement-profile vocabulary the tooling corpus already carries — *casual / careful / critical*. R5 is its measured instance. That vocabulary is the readiest-made input to ch. 11's profile set and appears in no segment. | ch. 11 [[instance-profiles]]; ch. 4 [[gates-need-destinations]] |
| **R22** | Two housekeeping facts worth not re-discovering: the 2026-07-23 generalization has **twin homes** (`arch/notes/` + `MOVED/udon/.../refine-more/`, byte-identical — a live collision surface if either is edited), and `_self/PRACTICA.md` is a **stub only** (system list item, no body). | ch. 3 [[history-layer]] / housekeeping |

### From the udon-theory copies (archived — cite the live corpus)

| # | Residue | Owed to |
|---|---|---|
| **R23** | **Proxy discipline has no home in this outline.** `norm-proxy-discipline` (live at `~/src/MOVED/udon/v2/theory/src/`) holds that every artifact that is not a re-derived claim is a **proxy**: outline rows, `:see` pointers, status labels assigned without opening a primary, multi-agent agreement, "the spike concluded," "verified against file F." Proxies may **locate** and cheap-screen; they may not be **cited as warrant**. Two named traps: **index as authority** ("the outline says X" is evidence about a navigator, not that X holds) and **verified-against-artifact** (checking that a file asserts *P* verifies the file, not *P*). ch. 3's [[warrant-over-authority]] is adjacent but is about *schema design*; this is about *what may serve as warrant* — and it is the norm this project's own INFLUX-do-not-cite rule instantiates. Companion: honest incompleteness as the discharge (an honestly lower tier + a stated strengthen-action + remainder released as a bare gap **is complete work**); proxy discipline without that discharge produces verification regress. | ch. 3 [[warrant-over-authority]] or its own norm row |
| **R24** | Smaller, for when the relevant rows draft: the identity/ordering split **does not require claims to form a partial order** (vivarium's FORMAT says outright it "does not have that ordering, and it may never want the same gating") — so the dependency DAG may be a per-domain addition rather than shared core, untested by removal; and the **competing formulations** worth stating when [[slug-identity]] drafts — path-encoded order (rename cascades) and pure-graph-without-outline (cold-start readers still need *some* exposition order; an outline is one answer, not the only one). | ch. 1 [[slug-identity]]; ch. 2 [[outline-as-organizing-principle]] / [[dependency-order-tension]] |  **DISCHARGED 2026-08-06 → [[slug-identity]]** (both competing formulations and the no-partial-order point are in its body, §The honest competitors)
| **R25** | The **bridge/gloss segment kind** was reached for independently by three instances (asf's cadence exemption for chapter-intro and most `disc-*`; `for-OUTLINE.md`'s intro-gloss and `impl-*` discussion-gloss; TST-extension-memo §6b proposing a `bridge` file-kind) — and §6b **priced it as the largest extension requested**. The ch. 9 gap row already names bridge/gloss kinds; this is the evidence behind the row, including the caution that the three may have inherited it from each other rather than converged. | ch. 9 gap (bridge/gloss segment kinds) |

---

## Working Notes

- The register is deliberately routed by **OUTLINE row**, not by source note, so that dispatching the remaining notes does not orphan it. Rows retire on landing.
- R12 (autopax retrofit), R13 (the two independent-corroboration legs), R23 (proxy discipline) and R4 (absence law written and never run) are the four this pass would flag as most likely to change a decision rather than fill in a body.
- Unexamined by this pass: `asf/`, `udon-analysis/`, `tribunal/`, `vera/`, `build/`, `methodology/`, and most of `instances/`. `udon-analysis/` in particular is named as the source of several items above (doc-store §12.2, underlying-logical-model, tst-grounding Appendix A) and is the obvious next dispatch target.

---

## From `residue-asf-build-2026-08-05.md`

# Residue register — `INFLUX/asf/` + `INFLUX/build/` (2026-08-05)

*Content in the twelve dispatched notes that the segments drafted from them do **not** carry, routed by the OUTLINE row it is owed to. Present-truth surface about what is owed: retire a row when its content lands in a segment body or is expressly rejected.*

*Every source below is byte-identical to (or a verified extract of) a **live** file that remains the authority; the live path is given so the residue survives the influx copies being archived. Verification marks: ✔ = checked first-hand against the live tree on 2026-08-05.*

---

## Owed to Chapter 1 (atom)

| # | Residue | Owed to |
|---|---|---|
| **A1** | ✔ **The pattern's own origin document, and the prediction it got wrong.** `~/src/arch/asf/_obs/old-tf-scratch-09-restructure-plan.md` is the pre-OUTLINE argument for one-result-per-file, and it argues the move on *navigation* grounds: monolithic documents needed dependency DAGs, results indexes and a notation appendix *"BECAUSE the document structure doesn't self-document,"* whereas under one-result-per-file *"The document structure IS the navigation. No index needed to find where theorems live."* The corpus that resulted has an `OUTLINE.md` per component, a `bin/lint-outline`, accepted-exception whitelists and generated indexes. So atomization did not remove the exposition layer; it relocated it into a view — which is the strongest in-estate evidence for why an outline is not an index, and it is evidence *against* the founding instance's own stated expectation. Reaches ch. 2 as much as ch. 1. | [[atom]] / [[outline-as-organizing-principle]] — arguably its own obs |  **DISCHARGED 2026-08-06 → [[navigation-relocation-specimen]]** (new appendix obs carrying the document's quotes and the resulting corpus's counts; the claim half is in [[outline-as-organizing-principle]]. The ch. 1 routing is *not* separately landed: [[atom]] carries only a Working-Note pointer, deliberately — the origin document's argument is about the exposition layer, which is a ch. 2 claim)
| **A2** | ✔ **Cross-references below segment grain.** ASF carries a second identity tier: named atoms (equations, tables, named formulas) get a kebab-case reference tag and resolve by `#name` independent of positional number — *"cross-refs always target intrinsic identity (slug for segments, name for atoms), never positional numbers"* (`~/src/arch/asf/FORMAT-TODO.md`, *Cross-reference evergreen-ness principle*). Author-side parsing shipped; the label emission that completes it is still queued. [[identities-over-locations]] states the norm at record grain only; the sub-record tier — and the fact that it is the half still unfinished after a year — is unstated anywhere. | ch. 1 inner-section gap; [[identities-over-locations]] |

## Owed to Chapter 2 (order)

| # | Residue | Owed to |
|---|---|---|
| **A3** | ✔ **A shipping relation-keyed exception store, in the exact shape the row anticipates.** ASF's deliberate ordering violations live in per-component `OUTLINE-accepted.md` whitelists, keyed by slug-pair, one row per exception carrying a reason and a record citation (the convention is per-component; exactly one such file exists today, `01-aat-core/OUTLINE-accepted.md`); accepted violations are **still printed**, marked accepted, and the linter's exit stays green (`~/src/arch/asf/doc/sop/agents.sop.md` §File Organization). Also there: frontmatter↔OUTLINE stage drift is reported **warning-only**, on the stated ground that stage is process-state, not epistemic strength. The row asks "what an accepted violation is" and a live answer exists. | [[dependency-order-tension]] |
| **A4** | **Rendering directives and label assignment as build-side evidence for the edge reading.** The neurips manifest's `Type` column is read by the build to inject `\appendix` before the first appendix-typed row and `\newpage` before a checklist row; the ASF assembler computes each segment's rendered label from outline order into its own index and chunk metadata, never into the source; no segment in either corpus carries a manual section/equation/theorem number, the recorded reason being that the predecessor workspace's manual numbering *"broke under reorganization."* [[view-edge-metadata]] (drafted in parallel) argues the edge reading from the logical model and measures stage-denorm drift; this build-side leg is complementary and absent from it. Details and quotes are in [[build-forced-commitments]]; this row is a note that the claim segment could absorb them on its next edit. | [[view-edge-metadata]] |

## Owed to Chapter 3 (steady state)

| # | Residue | Owed to |
|---|---|---|
| **A5** | ✔ **A worked layered-navigator model with per-file audience declarations.** ASF's root file map (`~/src/arch/asf/doc/sop/agents.sop.md` §File Organization) is the estate's most developed instance of tracking-by-altitude: navigator (`PRACTICA.md`, declared auditor-safe) → tactical (`TODO.md`) → portfolio (`PROPOSALS.md`) → history split into forward-going changelog and frozen pre-cutoff archaeology → generated public surfaces → a short steward-decision queue that holds *pointers only*, with work items staying in their home trackers. Six files carry an explicit **auditor-hidden** marking and three surfaces are marked **auditor-safe**; one file self-declares as priming-heavy. Two rows want this: the altitude ladder, and the audience declaration. | [[tracking-altitudes]]; [[priming-discipline]] (ch. 5) |  **PARTIALLY DISCHARGED 2026-08-06 → [[steward-surfaces]]** (audience-declaration half; the altitude ladder still owed to [[tracking-altitudes]])
| **A6** | ✔ **The notes store as an unowned decision queue, with named parked items.** ASF's 2026-07-07 lifecycle review found theory-correctness findings (a possibly-vacuous leakage bound, a hard-ceiling schema convention) parked in per-segment Working Notes awaiting steward routing, with no queue semantics and no owner — *"load-bearing theory-correctness items are parked where nothing watches them"* (`~/src/arch/asf/msc/meta-process-review-2026-07-07/01-theory-content-lifecycle-findings.md`). [[working-note-lifecycle]] states the general prohibition; the specific specimen and the named items are not carried. | [[decision-records]] |

## Owed to Chapter 4 (flux)

| # | Residue | Owed to |
|---|---|---|
| **A7** | ✔ **A harvest abandoned mid-motion, with the abandonment measurable from the artifact.** The "incidental audit gold" sweep lifted audit findings into per-segment notes across one of four components, stopped 2026-05-31, and its own onward leg — promoting that material into segment prose — never produced a commit. Its tracker's "next wave" section is entirely undone, and the absence is confirmable without reading the tracker: 0 gold blocks exist outside `01-aat-core`. This is a clean metabolism specimen where the *shape of the residue itself* reports the process state. Counts are in [[working-notes-deluge]]; the metabolism reading is not carried anywhere. | [[integration-metabolism]]; [[observable-crossings]] |

## Owed to Chapter 6 (releasing)

| # | Residue | Owed to |
|---|---|---|
| **A8** | ✔ **Per-artifact independent versioning, decided and shipping.** Each ASF volume carries its own semver in its own `mono-meta.yaml`, with major/minor/patch as separate keys so a tool can read and bump them, and explicitly no repo-level `VERSION` file — *"Each Volume has its own semver, independent of siblings — AAT can be v1.0 (stable, citable) while LogA is at v0.3 (still evolving)"* (`~/src/arch/asf/FORMAT-TODO.md`). The released filename carries the semver; the build stamps git sha and date into the artifact's own frontmatter. This is a concrete answer to "what does a release identify?" over a substrate whose records keep moving, and no segment carries it. | [[lifecycle-events]] |
| **A9** | ✔ **Cross-artifact references between independently-versioned releases.** ASF commits each volume's LaTeX `.aux` so sibling volumes can resolve cross-volume references at build time, and the open item names the failure mode precisely: what to render when a sibling's `.aux` is missing or was written against a **different semver** than the one being referenced (fallback forms proposed: bibliography-form citation, a marked placeholder, or a soft reference). Staleness detection between released artifacts is the general problem; this is a live unresolved instance of it. | [[lifecycle-events]] / [[pipeline-seams]] |

## Owed to Chapter 9 (pedagogy)

| # | Residue | Owed to |
|---|---|---|
| **A10** | ✔ **Respectful pedagogy, stated as an ordering discipline with an honesty constraint.** From `~/src/arch/asf/doc/sop/agents.sop.md` (Working Conventions): framing prose leads with the mental model — *"a preamble to the preamble"* — and gives precise structure as a second layer; the analog must be *"isomorphic, not merely evocative"*, meaning a reader who perturbs the analog gets predictions that hold against the formalism, with a worked four-way mapping given; *"scaffolding that overclaims is worse than none"*; and Layer 0 stays one tight paragraph — *"scaffold then precise," not "explain everything twice."* The Feynman criterion is institutionalized in a schema field, with the diagnostic being that the canonical example came from *a sympathetic outside reader working it out for himself on first encounter*, and the candid admission that most segments do not meet the standard. This is the richest pedagogy material in my chunk and both ch. 9 rows want it. | [[pedagogy-layering]]; [[plain-language-briefs]] |  **DISCHARGED 2026-08-06 → [[pedagogy-layering]]**
| **A11** | ✔ **The audience-layer proposal and its named risk.** SP-5 (`~/src/arch/asf/_obs/architectural-proposals-2026-04-22.md`, reopened at `PROPOSALS.md`, §H item 5): a per-segment 1–2 sentence preamble stating load-bearing content *without* qualification, ahead of the formal apparatus, on the ground that honesty apparatus produces heavy reading load. Its self-named risk is the one worth keeping: *"pressure to write a tight Reader's Path may push authors toward overclaiming-for-concision,"* so the convention must require load-bearing-but-not-overclaiming. Status as of that portfolio: unexamined. [[selection-and-projection]] carries the layering discipline; this is the specific layer and its failure mode. | [[pedagogy-layering]]; ch. 9 gap (bridge/gloss kinds) |  **DISCHARGED 2026-08-06 → [[plain-language-briefs]]**

## Owed to Chapter 12 (publishing mechanics)

| # | Residue | Owed to |
|---|---|---|
| **A12** | ✔ **The addressable intermediate, and what it buys.** ASF's pipeline materializes one normalized **chunk** per atom plus an index between source and output (`~/src/arch/asf/msc/markdown-first-pipeline.md`): each chunk is standalone readable markdown with a `**Key**: value` metadata block whose grammar is a stated, enforceable contract; the index records a source hash per chunk so only changed chunks regenerate, making incremental build intrinsic rather than bolted on; cross-references are left unresolved *in* chunks and resolved at assembly so a chunk survives reordering. The design's own claim for why the intermediate earns its place: it is *"the boundary where authoring discipline becomes machine-readable,"* and the named future consumers are an HTML/EPUB renderer, a search index, and an LSP-for-segments. Also there: the one genuinely position-dependent transform is **header bumping** (a segment's H1 lands at H4 in main matter, H3 in an appendix), so moving a segment between the two regenerates its chunk. I considered and did not draft an `assembly-intermediate` segment; the ch. 12 gap row is the natural home. | ch. 12 gap; [[semantic-indexing]] |
| **A13** | ✔ **Type-driven rendering.** The early LaTeX-build plan (`~/src/arch/asf/_obs/build-latex-plan.md`) proposed mapping a segment's `type:` to a theorem environment and its slug to a label — i.e. the record's own type vocabulary drives its rendered form. The shipped pipeline does a version of this (a segment header command carrying type, title, status and stage). Small but load-bearing for a generator: the local type vocabulary ([[type-vocabulary-locality]]) is not only an authoring nicety, it is an input to every renderer, which is one reason it cannot be standardized away. | ch. 12 gap; [[type-vocabulary-locality]] |

## Owed to Chapter 14 (metrology)

| # | Residue | Owed to |
|---|---|---|
| **A14** | **An instrument whose headline number is ~100% expected noise** (as reported by the 2026-07-07 review; the raw numbers below are that review's, not a current re-run — an adversarial re-run on 2026-08-05 against the root outline produced different counts, so treat 17/65 as a dated snapshot and the *shape* of the finding as the durable part). ASF's outline linter reported 17 "ordering violations," *all 17* of which were meta-segments deliberately placed at section openings under an introduced-before-used discipline — *"the tool's headline count is ~100% expected-noise an agent must know to discount"* — while the same tool's 65 "backmatter references" were correctly separated as intentional. Two further instrument gaps from the same review: no tooling compares an OUTLINE row's hand-authored status column against the segment's own frontmatter, which is why six rows in one component sat marked `missing` while the files existed with substantive content; and the note-admission discipline has no linter at all. This is a different failure from the taught-blindness hazard already registered (an instrument told to ignore things): here the instrument reports faithfully and the *signal* is worthless, which is the thing that trains agents to discount instruments generally. | [[corpus-instruments]] |  **DISCHARGED 2026-08-06 → [[corpus-instruments]]**

---

## Working Notes

- Routed by OUTLINE row rather than by source note, so archiving the influx copies does not orphan the register. Rows retire on landing.
- **A1, A10 and A14** are the three this pass would flag as most likely to change a decision rather than fill in a body — A1 because it is counter-evidence from the founding instance, A10 because it is a complete pedagogy discipline nobody has to invent, A14 because it names an instrument failure mode distinct from the one already on the outline.
- Not carried anywhere and deliberately dropped: the internals of the ASF build's LaTeX target (kaobook vs scrbook divergence, preamble fragments, sidenote/margin-note conventions, table polish). These are genuinely instance-specific typesetting concerns; if a generator ever needs them it should re-derive from `~/src/arch/asf/FORMAT-TODO.md` rather than inherit a summary.
- The 2026-07-07 lifecycle review's stage/status distributions are superseded by the counts re-run on 2026-08-05 in [[working-notes-deluge]] and [[ladder-never-fired]]; its *process* findings (a)–(f) are not superseded and are the source of A6, A7 and A14.

---

## From `residue-udon-analysis-2026-08-05.md`

# Residue register — `udon-analysis/` + three held-back notes (2026-08-05)

*Companion to [`INFLUX-DISPATCH-2026-08-05.md`](INFLUX-DISPATCH-2026-08-05.md), same shape and same rules: a present-truth surface about **what is owed**, routed by OUTLINE row, retired when the content lands in a segment body or is expressly rejected. Numbering continues from that register (R1–R25) to keep the two citable side by side.*

*Covers the pass over `INFLUX/udon-analysis/` (5 files), `INFLUX/synthesis/relata-methods-for-verisectorium-2026-08-05.md`, `INFLUX/synthesis/live-state-field-reports-2026-08-05.md`, and `INFLUX/instances/deployments.md`. Nine segments were drafted from this material; what is registered below is what did **not** land.*

**Verification marks.** ✔ = verified first-hand this pass against live trees. (rep) = reported by an INFLUX note at that note's own standing, not independently checked here.

---

## Corrections to the prior register

| # | Correction |
|---|---|
| **C1** | ✔ **The live udon theory tree is `~/src/arch/firmatum/udon/`, not `~/src/MOVED/udon/`.** The dispatch register (rows for the archived udon-theory copies, and R23) directs readers to `~/src/MOVED/udon/v2/theory/src/` as the citable live corpus. The two trees' `theory/src/` segments and the files cited by this pass are byte-identical, but they are separate git repositories with divergent histories and are *not* identical as trees: firmatum is one commit ahead (2026-08-05 vs 2026-07-31) and has collapsed a directory MOVED still carries. Anything citing MOVED as "the live corpus" is citing the stale twin. Two drafted segments — [[collision-staleness-detection]] and [[substrate-independence]] — carry MOVED paths in Working Notes and want repointing. |
| **C2** | ✔ **R1's stage-denorm number re-derived independently.** On `01-aat-core`, 2026-08-05: 169 OUTLINE rows carrying a slug, 168 compared against an existing file, **0 mismatches**, 1 row with no file, 170 segment files. Consistent with R1; now stated in a segment body ([[view-edge-metadata]]) rather than only in a register. |

## What landed (so these are no longer owed)

Eleven segments drafted this pass, all from this chunk — nine chapter-level claims: [[atom-as-cluster]], [[write-semantics-declaration]] (ch. 1) · [[view-edge-metadata]] (ch. 2) · [[influx-queues]], [[layer-speeds]] (ch. 4) · [[priming-discipline]] (ch. 5) · [[evidence-ledgers]], [[verification-provenance]] (ch. 8) · [[partition-isolation]] (ch. 13) — plus two appendix-grade observations carrying their counts and method, [[stage-denorm-zero-drift]] and [[gate-profile-divergence]]. Prior-register rows partially discharged by them: **R1** (into [[view-edge-metadata]]), **R15**'s convergent-lock rule (stated in [[evidence-ledgers]] §D, now citable), **R16**'s layout-vs-visibility split (into [[atom-as-cluster]] §B), **R19b** (events forced by two constraints — [[atom-as-cluster]] §C), **R21**'s gate-or-warn (into [[verification-provenance]] §D). Those five are *stated*, not exhausted; the register rows should be read against the segment bodies before retiring.

---

## Residue register

### From `udon-analysis/underlying-logical-model.md`

| # | Residue | Owed to |
|---|---|---|
| **R26** | **The corpus-verbs hypothesis, with its own counter-evidence attached** — that most agentic tooling would work better over the *logical* corpus (resolve, select, upsert-with-expectations, census) than over the file-layout perspective its verbs currently speak. Named as Joseph's hypothesis, pre-validation, and then *narrowed by its own owed prior-art pass*: the grep counter-lesson (indexed alternatives lost to staleness and self-correction; an editing agent invalidates its own index by construction), UDON's sigils already doing the structural tier's work lexically, and the surviving defensible claim being **containment and reference-following, not search** — 46% of one corpus's status attributes measured on a different line than their element, so *which record does this belong to* is unrecoverable lexically. Adoption evidence exists (relata's verb topology, picked up intuitively). This is a whole design thesis with a designed test (put both verb sets in front of working agents and watch which they reach for) and it has no row. | ch. 12 gap or a new row; possibly ch. 11 [[generator]] |
| **R27** | **The temporal axis splits from the spatial one, and the split is diagnostic.** For *space*, native iteration (grep) is natural and beat the built indexes. For *time*, the native instruments are serving-indexed while the questions are record-indexed: `git blame` answers about lines in files; the question is "who set this status, when, knowing what?" Standing steward observation that agents essentially never reach for git's mining verbs unaided. The estate's own contrast is the embryo — relata's per-record decision history (addressable) versus a 1,405-line changelog (greppable, not addressable), with a commit-subject convention as the demand carving itself into the only available surface. | ch. 3 [[history-layer]]; ch. 12 [[semantic-indexing]] |
| **R28** | **The atomicity/coherence result, and its refutation, both worth having.** A proposed identification of TST's coherence with ACID's atomicity was **refuted on quantifier** by an adversarial pass (coherence is an expectation over changesets; atomicity is universally quantified — they agree on the bulk and give opposite recommendations on the rare invariant-bearing tail, which is where corruption lives), and replaced by something stronger: **atomicity is the feasibility constraint, coherence is the objective over the feasible region.** With two field decision procedures named (transaction chopping; invariant confluence) and the estate's never-span-placements practice identified as chopping's maximally conservative special case. Also: the field's repeated move where N-file atomicity is genuinely needed is one small named primitive — *stage N files that are not yet the truth, then do exactly one atomic thing that makes them so*. | ch. 13 gap; [[write-safety]] (D) explicitly parks cross-file transactions |
| **R29** | **Durability is not visibility.** Atomic rename gives visibility, not crash-durability; the directory-fsync that a mature database added for exactly this is omitted from the estate's `safe_write` lineage, and macOS `fsync` is weaker than its name. Residual exposure characterized as recoverable-from-git, not corruption. Also the `link()`-fails-loudly-on-collision detail — carried in [[partition-isolation]] §D but not the fsync half. | ch. 13 gap |
| **R30** | **Includes / composition as its own subject, with the pre/postfilter tier split.** A prefilter is bytes→bytes applied before recognition; a postfilter is model→model. Confusing them yields silent no-ops one way and impossible demands the other. Attached: the chain travels with the ascription (provenance *of transformation* — no known include mechanism records this); degradation is whole-or-inert; the inherited caution that whole-or-inert is not by itself a resource cap. The composition half of "living documents" was identified as the one property of that vision with **no chapter anywhere** in its own corpus. | ch. 12 gap; ch. 2 (a view's rows as includes with membership predicates) |
| **R31** | **Every markdown file is already a snippet with an empty attribute section** — so a `.md` corpus is a pre-existing population that is *interred*, not converted, with reversible auto-escape as the interring rule. Bears directly on what migrating an existing ad-hoc instance would actually cost. | ch. 4 whole-outline-migration gap; ch. 11 migration gap |
| **R32** | **The five-times prior, honestly flagged as possibly a rut.** Five separate design questions (write-governance, gating, contraction, interring, include-filtering) each resolved to *a declared role plus behavior at a membrane* rather than to language law — with the author's own caution that this is one conversation, two minds, and that the informative event would be a sixth case that **resists** it. Worth keeping as a stated prior with its falsifier attached rather than as an unnamed habit. | ch. 7 gap (which properties carry the effect); ch. 11 [[derivation-not-templates]] |

### From `udon-analysis/tst-grounding.md`

*(Adjudicated as faithful at the project's founding, and its citation discipline is already carried by [[first-principles-grounding]]. What remains is content.)*

| # | Residue | Owed to |
|---|---|---|
| **R33** | **The transfer table is the reusable artifact, not the conclusion.** TST's own governing segment obliges an exporter to name which of six epistemic properties the target domain shares, approximates, and lacks. Run for documents, the result is that documents hold four and lack two — executable counterfactuals and cheap characterized intervention channels — and the reading that a checker *manufactures* the missing channel is flagged by its own author as the claim he'd most want attacked. [[first-principles-grounding]] names this at citation level; nobody has stated the **obligation** as something a verisectorium exports under. | ch. 7 gap; ch. 8 [[epistemic-axes]] |
| **R34** | **Why late payment is multiplied, not merely later.** Comprehension cost compounds per-reader while implementation cost does not, so the read-time normalization a schemaless corpus defers is multiplied by the session count — and for fully-turning-over agents that count is the readership. With the closure worth keeping: *amortizing the read-time payment across readers just is paying it at authoring time, one reader late.* [[turnover-solution]] carries the turnover claim; nothing carries the asymmetry that makes structure-now a different kind of cost from structure-later. | [[turnover-solution]]; ch. 7 gap |
| **R35** | **More gates ≠ more assurance.** Channels only sum when their noises are independent, with saturation under shared persistent bias — so a schema layer, a linter and a CI check that all read the same declaration and can all be wrong the same way buy roughly one channel. This is the theory-side twin of [[evidence-ledgers]]' independence rule and is a hard design constraint on any multi-gate scheme. | ch. 4 [[gates-need-destinations]]; ch. 8 [[evidence-ledgers]] |
| **R36** | **The lower bound on paying early.** A specification-bound result says you cannot reliably produce the intended artifact before the distinguishing information has been transmitted — so early schema payment in an unstabilized domain is not a cheap investment but a fabricated constraint. Paired with the same theory's *strategic test timing* discussion (write the constraint **at the alignment event**, not before the model has stabilized), this is a stated resolution to the schema-timing question and it has no home. | ch. 11 [[generator]] / [[instance-profiles]]; ch. 6 [[lifecycle-events]] |
| **R37** | **Contraction's absence is a threshold artifact, not a domain fact** — derived in three steps from the theory's own segments (a contraction is a feature; without an expand-contract mechanism its cost is maximal in both size and scatter; so the inequality fails for reasons unrelated to whether the contraction is worth doing). With the author's own weakest-link flag: a tool-inflated cost and a genuinely-not-worth-it contraction produce the same observation, and only measuring the two terms separately distinguishes them. Bears on removal, retirement, and every "we never delete" observation in the estate. | ch. 3 [[working-note-lifecycle]]; ch. 6 gap |
| **R38** | **The under-attribution defect, as the mirror of overclaim.** A verified count: a bridge corpus of 30 chapters restates the theory's formulas correctly and cites **zero** of its slugs, while the deep report upstream cites each exactly once. Named as mechanical loss during compression, cheap to repair before landing. [[first-principles-grounding]] carries this in a Working Note; the general shape — *compression drops citations while preserving content, and the loss is invisible in the output* — is a corpus-health claim with no row. | ch. 14 [[corpus-instruments]]; ch. 12 gap |

### From `udon-analysis/de-novo-audit-generic.md`

*(Landed: [[priming-discipline]]. What follows is everything else in a 992-line SOP that the norm segment deliberately does not carry.)*

| # | Residue | Owed to |
|---|---|---|
| **R39** | **The serial-reflection cadence produces a different object, not a slower one** — read one record, write a reflection, then read the next, so that judgments become *commitments later records can refute*. Under batch reading, a judgment of record 12 formed with record 37 already in context is better-informed and **unfalsifiable**. The protocol is described by its own annotator as running the reader as an instrument whose surprisal is the measurement, with a falsifiable prediction attached: the gain should be large on cross-record drift and integration debt, near zero on local within-record errors. This is the whole of ch. 9's [[experiential-reading]] row, already worked, in a live SOP. | ch. 9 [[experiential-reading]] — close to a drafting brief |  **DISCHARGED 2026-08-06 → [[experiential-reading]]**
| **R40** | **Soft directives only work when the option is already in the agent's default set** — with a first-hand incident: the agent that co-wrote the SOP violated its per-record reflection rule within minutes, because "consider writing per-record files" read as a *flavor of writing* rather than a *cadence to interrogate*; the decision point never fired. Generalized by the annotator: *any intervention aimed at a disposition held across time loses to a default that fires per action*, which predicts that guidance naming a **moment** survives contact with work while guidance naming a **stance** erodes invisibly. Directly bears on every convention this project proposes to write down. | ch. 5 [[role-activation]]; ch. 11 [[derivation-not-templates]] |  **DISCHARGED 2026-08-06 → [[role-activation]]**
| **R41** | **The completion drive is given a smaller object rather than resisted.** Fifty records × one reflection each discharges the drive continuously instead of letting it accumulate into one premature deliverable; and the reflection quota makes not-thinking *more expensive* than thinking, inverting the usual economics where skimming is cheap. The matching move in the report spec: a rescinded-candidates section makes the rejection list itself a creditable deliverable, so an honest zero stops being an admission. [[session-cycle-ergonomics]] states that agents need cycles; this is the mechanism, plus its predicted failure (over-generating candidates in order to have something to rescind). | [[session-cycle-ergonomics]]; ch. 5 gap |
| **R42** | **The report as a routing artifact, with a five-tag disposition vocabulary** (new · known-unintegrated · known-resolved · tooling gap · scope/status mismatch) and the **integration debt vs theory gap** distinction behind it — the resolution exists somewhere but hasn't propagated, versus the work hasn't been done. Different remediation paths, different urgency, different signals about corpus health. Also the per-finding shape (verbatim passage · counter-evidence search · status · confidence · why-it-still-stands) and the anchor rule: an integrator should resolve the anchor in under 30 seconds, and line numbers are one acceptable form among several. | ch. 4 [[influx-queues]] (typed residue at report grain); ch. 15 [[authority-routing]] |  **MARK RETRACTED 2026-08-06** — a same-day `PARTIALLY DISCHARGED → [[authority-routing]]` mark was written here and was wrong: that segment routes *decisions*, a different mechanism sharing the word. The five-tag disposition vocabulary, the per-finding shape, and the 30-second anchor rule landed in no segment (adversarial check also cleared [[influx-queues]]). **This entry stands fully open.**
| **R43** | **The gold lift: the second product, disclosed and deliberately not targeted.** Raw reflections are mined afterwards into the record's own working notes; the reader is told this *and* told not to switch into produce-gold mode, because targeting it destroys the first-encounter authenticity that makes it valuable. The detail that inverts the usual deliverable/reject relationship: the early conflation of finding-with-framing is **preserved as signal, not sanitized** — so the second product's yield partly consists of the first product's errors, and a tidying pass destroys the material rather than curating it. Routing rules attached: organize by the record each thought is *about*, and match by content, never by note-number, because numbering drifts across cycles. | ch. 9 gap (wondering-notes as harvestable pedagogy); ch. 3 [[working-note-lifecycle]] |
| **R44** | **Comprehension and judgment must live in the same agent; which agent that is, is free.** The reconciling principle behind an apparent contradiction in the SOP (delegation is abdication / sanctioned multi-pass mode where sub-agents each run a whole audit). Splitting them across the boundary fails in either direction. Stated by the annotator as the general rule and explicitly *not* stated in one place in the source — the kind of apparent inconsistency that licenses routing around the discipline. | ch. 5 [[work-coordination]] |  **DISCHARGED 2026-08-06 → [[work-coordination]]**
| **R45** | **A self-monitoring budget gate depends on the faculty the condition degrades** — and the annotator's read is that the number does nothing while the *permission* ("asking for a second pass is normal and virtuous") does all the work. Worth knowing before this project writes any threshold into a generated instance. | ch. 5 gap; ch. 11 [[instance-profiles]] |

### From `synthesis/relata-methods-for-verisectorium-2026-08-05.md`

| # | Residue | Owed to |
|---|---|---|
| **R46** | **Gate dispositions and tribunal records are one schema family at three altitudes** — micro (a working-note disposition), meso (a promotion decision), macro (a governance decision) — sharing columns: subject, verdict, ledger-or-reasons, load-bearing reasons, revisit-when, expires-on, actor, timestamp. With the concrete finding that the middle rung is the unbuilt one, and a written-out event shape ready to use. This is R17 from the prior register arriving from a second, independent direction, with the schema supplied. | ch. 4 [[observable-crossings]] — the closest thing to a drafting brief in either register |
| **R47** | **`pending` as an operable queue, not another markdown list.** Unfinished work in this estate scatters across outline gaps, working-note deluges, TODO files, parked lifts and lint noise; the shipped counter-example makes wait-states a **query** over event stores and gaps, each row carrying the exact next command. | ch. 15 [[steward-surfaces]] / [[authority-routing]]; ch. 3 [[asked-and-answered]] |
| **R48** | **Rename survival wants an alias table, and same-claim-different-expression wants a link that never merges.** Shipped form: absorbed keys live on as aliases, emission writes under the *cited* name, and citation-distinct versions link rather than merge — with a measured failure behind it (silent mint-on-collision produced 29 duplicate clusters) and a regrowth gate added after a re-drain recreated twins within hours. [[slug-identity]] and [[identities-over-locations]] give identity stability; neither gives rename *survival*. | ch. 1 gap (restatements, families, aliases); [[slug-identity]] |
| **R49** | **Coverage honesty as a first-class field.** A registered artifact may be a sample, a chapter, a preprint; claiming `full` when sample cues fire is forbidden. The claim-space analogue — bridge segments, partial bodies, excerpt recapitulations standing in for full arguments — is exactly how a verifier ends up checking against the wrong object with high confidence. | ch. 8 [[epistemic-axes]]; ch. 1 gap |
| **R50** | **The designator ladder — never silent resolution.** Five tiers from automatic through blocking-interactive, batch choices, a pending report, and a spool queue; with three acts kept distinct that are routinely conflated (re-present the stored memo ≠ recompute the pipeline ≠ decide). Claim-space reading: a cross-reference resolved by best guess is a false-accept in theory clothes, and renamed slugs and accepted-violation exceptions want the same ladder rather than a line in a lint noise floor. | ch. 2 [[dependency-order-tension]]; ch. 12 [[semantic-indexing]] |
| **R51** | **Cost-ordered work with a principled early-stop, and its inverse.** Stages ordered by cost; stop early only when the posterior clears threshold *and* nothing stands refuted *and* the margin survives what pending work could yield. Parallelism changes when evidence arrives, never how the decision is defined. The inverse is the one that matters here: **a standing contradiction should expand the work, not shrink it.** | ch. 5 [[work-coordination]]; ch. 8 [[evidence-ledgers]] |  **PARTIALLY DISCHARGED 2026-08-06 → [[work-coordination]]** (contradiction-expands-work half; cost-ordered-stages / early-stop half still open)
| **R52** | **Observation-wide, model-careful.** Record every deterministically-knowable process feature append-only *before* knowing how to weigh it; the model is a consumer. Named as the instrumentation program that would make multi-timescale strata measurable instead of metaphorical — gate outcomes, disposition acts, co-edit statistics, defect classes, time-in-state. The ch. 14 row already names this; the argument for recording before weighting, and the honest note that the source's own calibration store was nearly empty while its classes existed, are what's missing. | ch. 14 [[observation-stores]] |  **DISCHARGED 2026-08-06 → [[observation-stores]]**
| **R53** | **The framing correction, stated plainly: relata is not "refs generation four."** Treating it as a cousin store captures its layout and misses its product, which is a calibrated belief-update discipline rehearsed on a deliberately safe domain. Also its honest inverse, which the segments should not lose: relata is *not* a better verisectorium — it has no authored pedagogical view, no present-truth claim bodies, no claim-kind vocabulary, and no analogue of slow ordered reflection. | ch. 11 [[instance-profiles]]; complements R11 in the prior register |

### From `synthesis/live-state-field-reports-2026-08-05.md` and `instances/deployments.md`

*(Both were deliberately held in INFLUX by the prior pass as still-residue-bearing; R1–R11 already register most of it. Added here:)*

| # | Residue | Owed to |
|---|---|---|
| **R54** | **The deployment spectrum is itself a finding, including its negative half.** Full claim-verisectoria, directory-as-table cousins without a pedagogical outline, paper-section files with ordered filenames and no epistemic ladder, and pre-pattern projects — plus the judgment that ordered `NN-`prefixed section files are the pattern *lite* and violate slug-without-number, and the explicit not-a-verisectorium list kept so nothing is rediscovered as missing. [[type-vocabulary-locality]] and [[view-genres]] carry pieces; the spectrum as a spectrum, with its boundary cases, has no row. | ch. 11 [[instance-profiles]]; possible new survey row |
| **R55** | **A narrative-order outline with non-authoritative connective prose** (comproprium's chain view) is a live counter-case to outline-as-mere-ordering, and a **harvester-brief genre** exists as its own artifact kind. Both are family members no roster listed. | [[view-genres]] (append when next edited); ch. 5 gap |

---

## Dispositions for this chunk

| File | Destination | Why |
|---|---|---|
| `udon-analysis/underlying-logical-model.md` | `.archive/udon-analysis/` | ✔ byte-identical (modulo the provenance banner) to the live original. Copy of live authority — archive-shaped by construction. Cite `~/src/arch/firmatum/udon/v2/theory/to-integrate/primary/underlying-logical-model.md` (see C1). |
| `udon-analysis/tst-grounding.md` | `.archive/udon-analysis/` | ✔ byte-identical to its live original, same tree. Adjudicated as faithful at founding; residue R33–R38 registered above. |
| `udon-analysis/living-documents-seed.md` | `.archive/udon-analysis/` | ✔ byte-identical to the live original (`.../refine-more/living-documents/README.md`). Residue R30, R31. |
| `udon-analysis/de-novo-audit-generic.md` | `.archive/udon-analysis/` | ✔ byte-identical to the live original (`.../refine-more/de-novo-audit.md`), which is itself a genericized copy of the live ASF SOP at `~/src/arch/asf/doc/sop/audit.sop/de-novo.sop.md`. [[priming-discipline]] cites the ASF SOP directly; residue R39–R45. |
| `udon-analysis/doc-store-report-s12-2-outline-segments.md` | `.integrated/udon-analysis/` | An **extract**, not a full-file copy — the only file of the five that is not byte-identical to anything live. Its §12.2 content is absorbed into [[view-edge-metadata]] and [[verification-provenance]]; the live original is `~/src/arch/firmatum/udon/v2/theory/to-integrate/refine-more/doc-store-and-schemas-report.md` §12.2. |
| `synthesis/relata-methods-for-verisectorium-2026-08-05.md` | `.integrated/synthesis/` | Not a copy of anything — a commissioned comparative report, with no live original. Its §§2.2–2.8 and 2.11 are absorbed into [[evidence-ledgers]], [[influx-queues]] and [[verification-provenance]]; its remaining transfers are registered R46–R53. The live authority for every relata claim is the relata tree itself. |
| `synthesis/live-state-field-reports-2026-08-05.md` | **stays in INFLUX** | Unchanged from the prior pass's judgment: R1–R11 remain largely unlanded and several of its ✔ numbers are load-bearing for undrafted ch. 3 / 8 / 11 / 14 rows. R1 is now discharged into a segment body; the rest are not. |
| `instances/deployments.md` | **stays in INFLUX** | Unchanged from the prior pass's judgment: a live-useful roster still being consumed, and R54–R55 above are additions to its owed list rather than a discharge of it. |

**Honest incompleteness.** This pass read its eight assigned files whole and verified the load-bearing numbers, quotes and tiers against live trees. It did not adjudicate `asf/` (11 files), `tribunal/` (14), `vera/` (22), `build/`, `methodology/`, or the rest of `instances/`.

---

## From `residue-tribunal-2026-08-05.md`

# Residue register — `INFLUX/tribunal/` — 2026-08-05

*Real content in the tribunal notes that no drafted segment carries. Routed by the OUTLINE row it is owed to, so it can be discharged when that row drafts. Companion to `INFLUX-DISPATCH-2026-08-05.md` (same shape, different chunk); rows retire when their content lands in a segment body or is expressly rejected.*

**Register marks.** ✔ = verified first-hand this pass. (inherited) = reported by an influx document at its own stated register, not re-checked here. Where a note's live original still exists, the live path is given — cite that, never the influx copy.

---

## Dispatch record

| Note | Destination | Why |
|---|---|---|
| `tribunal-revisited-2026-07-29.md` | `.integrated/tribunal/` | The load-bearing document. Its §1 (roles, decision≠confidence, revisit/expires, hypothesis-coupling), §2 (typed voices, register-collapse), §4 (literature inheritances + capture problem + scoped novelty claim), and §6 (the vivarium specimen) are carried by [[decision-records]], [[tribunal-record]], [[warrant-over-authority]], [[asked-and-answered]], [[rationale-capture-survey]], [[authority-flag-specimen]]. ✔ live original at `~/src/arch/firmatum/udon/v2/theory/to-integrate/refine-more/epistemic-tribunal-revisited.md` remains authority; residue R-T1…R-T5 below. |
| `synthesis-scatter-2026-08-05.md` | `.integrated/tribunal/` | Its strand map and non-collapse warning are now carried durably by [[tribunal-strand-survey]], with live sources per strand. This gather-map has no live original — it was written for the gather — and its content survives in the survey. |
| `discovery-internal-architecture-2025-09-21.md` | `.integrated/tribunal/` | Strand B's structural content (four aspects as one mind, graded invocation depths) is in [[tribunal-strand-survey]]. The remainder is ELI-cohort record, not this project's material, and stays with ✔ live original `~/src/_core/sapientia/zi-am-tur/2025-09-21-epistemic-tribunal-discovery.md`. |
| `gate2-probes-discussion.md` | `.integrated/tribunal/` | The three probes, the known-unknown/unknown-unknown argument, and the worked example are carried by [[discussion-probes]]. ✔ live original `~/.claude/memory/epistemic-discipline/gate2-probes-discussion.md`. |
| `feedback-gate2-epistemic-tribunal.md` | `.integrated/tribunal/` | The memorata-principle twin of the above; same content, older framework name (AAD); cited in [[discussion-probes]]. ✔ live original `~/src/memorata/memory-curation/epistemic-discipline/feedback_gate2_epistemic_tribunal.md`. Nothing unique. |
| `zoetica-tribunal-xml-template.md` | `.integrated/tribunal/` | Six-line XML template; its content is the strand-B invocation shape, referenced in [[tribunal-strand-survey]]. ✔ live original `~/src/_core/zoetica/.archive/docs-20251012/process/templates/tribunal.md` (already inside an archive). |
| `ref-project-README.md` | `.integrated/tribunal/` | Strand A's shape (four agents, five document categories, stakes tiers, sandboxing) is carried by [[tribunal-strand-survey]]. ✔ live original `~/src/_ref/epistemic_tribunal/README.md`; its `src/` tree is the primary for anything deeper. |
| `collaborative-cognition-synthesis.md` | `.archive/tribunal/` | **Adjudicated as not-landing.** An integration essay (2025-09-07) merging strand A with a separate collaborative-cognition program. Its register is promotional ("revolutionary," "breakthrough," "complete framework") and its content is forward-looking design intention and a three-phase plan, not results; the phases were not executed as written. Named in [[tribunal-strand-survey]] precisely so it is not mistaken for a fifth design. ✔ live original `~/src/_core/synaptic/docs/primordial/epistemic_tribunal_collaborative_cognition_synthesis.md`. Two ideas in it are real and are registered as R-T6 rather than lost. |
| `ref-project-TECHNICAL_ANALYSIS.md` · `ref-documents/` | **stays in INFLUX** | Genuinely un-metabolized, and its residue routes almost entirely into **ch. 8 rows I did not draft** ([[confidence-calibration]], [[temporal-truth]], [[epistemic-axes]]) — plausibly another chunk's material (`INFLUX/vera/` is unadjudicated). Dispatching would move it out of the path of the agent who needs it. Residue R-T7…R-T9. |
| `00-INDEX.md` | **stays in INFLUX** | Directory map; still accurate for what remains. Updated in place with a dispatch note. |

**Honest incompleteness.** `ref-project-TECHNICAL_ANALYSIS.md` was skimmed by section headings and spot-read, not read whole; `ref-documents/` was read at the two document files plus the lexicon YAML head. Strand A's `src/` implementation was not read at all (it is code, and the live tree stands). Everything else in this chunk was read completely.

---

## Residue register

### From `tribunal-revisited-2026-07-29.md` (integrated — cite the live original)

| # | Residue | Owed to |
|---|---|---|
| **R-T1** | **Records that predate their own governance need an honest re-grading path.** Joseph on a 2026-07-12 entry tagged `:by us`: *"probably council before we had that as an option, more or less."* Retroactive classification at the grade the process *would have* assigned, **marked as retroactive** — never a silent upgrade. The source notes no verified ADR state machine covers this lifecycle case, which if it holds is a second small novelty beside prospective falsification. Named in [[decision-records]]'s Working Notes; asserted by no segment. | ch. 3 [[history-layer]] or [[decision-records]]; ch. 8 [[temporal-truth]] (re-grading records that predate their governance is already in that row's summary) |
| **R-T2** | **The decision-record schema as a candidate core schema, and its demand condition.** Joseph, at his own stated register (*"I suspect"*, pre-validation, deciding nothing): this may be one of the original built-in schemas — *"specifically for anything deliberative where the choices made by agents accumulate and get magnified and need to know what they are."* The demand clause is the useful part: it **selects the corpora** — decision ledgers, spec rulings, schema adjudications, migration calls — i.e. exactly where the archaeology cost gets paid when reasons weren't captured. Also the proposed composition with an epistemic-register schema: one marks a *claim's* standing, the other records a *decision's* provenance, and they compose (a decided register entry cites a tribunal record as its warrant). | ch. 11 [[instance-profiles]] / [[generator]] (which record kinds a profile ships); ch. 8 [[epistemic-axes]] |
| **R-T3** | **A named first customer with a deadline attached.** vivarium's council ledger surfaced its own growth question (full-history + generated projection vs status quo vs split) and Joseph routed the answer here: *"status quo until it hurts. We're actually coming up with a more principled approach right now as we speak over in udon."* Their named parts list: a **generated projection of currently-relevant decisions** (the canonical/derived split applied to a ledger — also the read-cost answer to a growing session tax), **citation guidance** for referring to a decision, and **event-routing** that keeps the ledger body present-truth while change-marks land addressably. ✔ the ledger is real and growing: **160 decision blocks / 1,697 lines** on 2026-08-05, up from ~130 / ~1,400 on 2026-07-29. "Until it hurts" is a commitment with a clock. | ch. 2 [[multiple-views]] (generated projection over a ledger); ch. 3 [[decision-records]]; ch. 12 [[derived-vs-authored]] |
| **R-T4** | **The de-facto schema is already 90% present, measured.** (inherited) The source reports vivarium's council decision entries at **99% / 91% / 88% child-presence** for their three sub-elements, measured 2026-07-29 — i.e. the council already writes decision-record embryos unprompted, and the delta to the fuller shape is close to exactly the adjudicator's three columns (load-bearing degree, revisit-when, expires-on). This is the estate's strongest *cheapness* evidence for the capture-escape argument, and [[decision-records]] carries the argument without the number. Re-measurable in one command. | ch. 3 [[decision-records]]; ch. 14 [[corpus-instruments]] |
| **R-T5** | **Two portable framing moves, unhomed.** (a) **Pre-registered revisitability as one family across altitudes** — an expectation bound attached to an *operation* before its outcome exists, and an expires-on attached to a *decision*, are the same move at two scales. (b) **A decision's revisit trigger has two sides**: premises going in (assumptions — disabling one fires the trigger, which the surveyed prior art already mechanized) and predicted outcomes coming back (the hypothesis side, which the surveyed argument-ground vocabularies apparently lack entirely). The second is a specific, checkable gap claim about the literature and would be worth confirming before it is repeated. | ch. 8 [[temporal-truth]]; ch. 1 [[write-semantics-declaration]] for (a) |

### From `collaborative-cognition-synthesis.md` (archived — cite the live original)

| # | Residue | Owed to |
|---|---|---|
| **R-T6** | Two ideas worth not losing with the essay. (a) **Institutional wisdom transfer as the design method**: the role-set was not invented but *copied* from domains under centuries of selective pressure for accuracy — legal cross-examination, scientific falsifiability, intelligence source-grading and competing-hypothesis analysis, medical differential diagnosis, historical source criticism, mathematical proof-checking. That is a real and reusable derivation argument for a role-set (and a source of role-sets nobody here has mined). (b) **The circular-authority problem stated cleanly**: authority metrics that rank by other authorities never bottom out, so the escape has to be non-recursive — evaluating *incentive structures* rather than reputation. Both are asserted, not demonstrated, in a document whose register overclaims throughout; treat as framings, not findings. | ch. 8 [[tribunal-record]] (role-set derivation); ch. 7 gap / ch. 11 [[derivation-not-templates]] |

### From `ref-project-TECHNICAL_ANALYSIS.md` + `ref-documents/` (still in INFLUX)

| # | Residue | Owed to |
|---|---|---|
| **R-T7** | ✔ **A 2025 in-estate instance of this very pattern, with machine-readable epistemic state in frontmatter** — and nobody has compared it to the current instances. Strand A's documents carry `confidence: 0.95`, `confidence_interval: [0.92, 0.97]`, `decay_rate`, `empirical_support`, `contradictions`, `last_updated`, plus `parent_principles` / `derived_principles` — i.e. **a dependency DAG and a calibration state in the record's own frontmatter**, in five declared types (First Principles / Lexicon / Derived Principles / Mental Models / Empirical Findings) explicitly ordered by rate-of-change from stable to fluid. That is a layer-speeds vocabulary and a type vocabulary in one, from a year before the current instances. **Sharp caveat: the numbers are seed-corpus demo values** (`empirical_support: 127`, "overconfidence in 89% of tested cases", a 2024 timestamp) — they are not measurements and must never be quoted as such. | ch. 1 gap (section-vs-frontmatter epistemics — this is a *third* position in the vivarium/udon-needs disagreement); ch. 4 [[layer-speeds]]; [[type-vocabulary-locality]]; ch. 8 [[confidence-calibration]] |
| **R-T8** | ✔ **Bayesian confidence machinery, specified in enough detail to adopt or reject deliberately**: Beta-distribution priors from point estimates, evidence weighting with **exponential temporal decay by half-life**, multi-agent aggregation as a weighted Beta mixture (with the challenger's opposition reducing confidence rather than averaging into it), and three named calibration metrics — **ECE, MCE, Brier score** — with binning. Whatever this project decides about numeric confidence, the decision should be made against this, since it is the estate's own most-developed attempt and it is sitting unexamined. | ch. 8 [[confidence-calibration]] / [[evidence-ledgers]]; ch. 14 [[observation-stores]] |
| **R-T9** | ✔ **Two smaller items.** (a) **Six named epistemic modes** — adversarial / falsification / probabilistic / diagnostic / formal / rapid — each with its source discipline, selected per claim; a mode vocabulary nothing current carries. (b) **A lexicon as a bounded context in YAML** (`ubiquitous_language` with per-term definition, type, constraints, usage, related-terms) — a concrete prior answer to the ch. 3 terminology-substore gap, which that row calls immature even in asf. | (a) ch. 8 [[epistemic-axes]]; (b) ch. 3 terminology/lexicon gap |

---

## Working Notes

- Routed by OUTLINE row rather than by source note, deliberately — dispatching the remaining files must not orphan this register.
- The three most likely to change a decision rather than fill in a body: **R-T3** (a real customer with a stated clock), **R-T7** (a prior in-estate instance nobody has compared against, and a third position in a live disagreement), **R-T2** (which corpora the decision-record schema is *for*).
- **R-T4** is the cheapest to discharge: one re-measurement converts an inherited number into a first-hand one.
- Flagged for adjudication, not decided here: none of this chunk's copies is the *only* home of its content — every dispatched file has a live original that still exists (✔ checked at dispatch). The one gather-native file (`synthesis-scatter-2026-08-05.md`) had no original, and its content is carried forward by [[tribunal-strand-survey]] before archiving.

---

## From `residue-instances-2026-08-05.md`

# Residue register — `INFLUX/instances/` + `INFLUX/methodology/` (2026-08-05)

*Content in the dispatched notes of this chunk that **no segment currently carries**, routed by the OUTLINE row it is owed to. Same contract as [`INFLUX-DISPATCH-2026-08-05.md`](INFLUX-DISPATCH-2026-08-05.md): present-truth about what is owed, not a history layer. Retire a row when its content lands in a segment body or is expressly rejected.*

**Chunk scope.** `INFLUX/instances/` (everything except `deployments.md`, owned elsewhere) and `INFLUX/methodology/`. **Twenty files were dispatched. 17 of them** carried a provenance banner naming a live source, and all 17 were verified byte-identical to that source (banner stripped, sha256 compared, 2026-08-05) — so they are archive-shaped by construction and **the live tree is the citable source**. Paths below are live paths, and re-reading them is cheap; nothing here should be cited from an INFLUX copy. The two exceptions: `instances/00-INDEX.md` was written by the gather itself, not copied (archived because its content is superseded — see N41), and two synthesis notes went to `.integrated/` rather than `.archive/` because drafted segments now carry them.

**Verification marks.** ✔ = re-run or re-read first-hand against the live tree on 2026-08-05.

---

## Owed to Chapter 1 — atoms

| # | Residue | Owed to |
|---|---|---|
| **N1** | **Rename survival has a working mechanism in this estate and no segment carries it.** relata keeps retired keys forever as `aliases:` on the survivor, `Entry.resolve` falls back to the alias index, and **`emit` writes each entry under the name the *consumer* cites** — so merges and renames never break a downstream build, with zero consumer edits. `validate` enforces alias uniqueness corpus-wide and every key-minting site checks the alias index, because a re-drain once recreated merged twins within hours. Live: `~/src/arch/firmatum/relata/README.md` §4. Adjacent to but not the same as [[name-collision-across-stores]]: that is one name over several things *between* stores; this is several names over one thing *within* one, and the estate has a working answer to the second and none to the first. | ch. 1 gap row (restatements, families, aliases, rename survival) |  **DISCHARGED 2026-08-06 → [[rename-survival-and-families]]** (§A and the regrowth-gate incident are in its body; the between-stores half stays with [[name-collision-across-stores]])
| **N2** | **"Siblings link, never merge" is stated twice, independently, on different atoms.** relata: citation-distinct versions (preprint vs journal, editions, translations) become distinct entries linked by `same_work_as:`, because a silent merge would corrupt citations. comproprium: *"Restatements are not duplicates. Two segments are duplicates only when they resolve to the same span in the same primary… Deduping on sentiment rather than span flattens a family into whichever version an early agent preferred"* — linked by `:family`. Both give a **mechanical duplicate test** (same span / same locators) rather than a semantic one. Live: relata README §4; `~/src/arch/proprium/comproprium/FORMAT.md` §D4. | ch. 1 gap row (same-claim-different-expression) |  **DISCHARGED 2026-08-06 → [[rename-survival-and-families]]** (§B; both statements quoted, with the mechanical-vs-semantic duplicate test as the shared substance)
| **N3** | **Order-in-filename is live in the estate as the competing design to slug identity.** ✔ `~/src/behavioral-floor/src/00-meta.md … 09-conclusion.md` and `~/src/causal-language/paper/src/00-…10-*.md`; the build simply globs `src/[0-9][0-9]-*.md` sorted, so there is no manifest to maintain — and inserting a section means naming it `04a-`. Cheap for a fixed venue skeleton; loses free reordering without renames. The honest statement of the trade belongs wherever [[slug-identity]] states its case. | ch. 1 [[slug-identity]] |  **DISCHARGED 2026-08-06 → [[slug-identity]]**
| **N4** | **Cite a decision by its exact slug — with a measured asymmetry behind it.** vivarium's FORMAT: a prefix is a dangling reference, not a short form; an ellipsis truncation resolves to nothing. The measurement: LEXICON references from `core/` dangled at **zero** while decision references dangled, "because only one of the two had a stated form to lint against." Also carried there, and uncarried anywhere here: the authoring craft of *naming* failing forms rather than quoting them, so a grep for real references does not collect the teaching examples — **a doc carrying a deliberate error is a trap for the next instrument.** Live: `~/src/arch/vivarium/FORMAT.md` §5.1. | ch. 1 [[slug-identity]]; ch. 14 [[corpus-instruments]] |
| **N5** | **`demonstration` as a segment kind, with its mechanism argued.** comproprium argues a demonstration is not an account with a happy ending and needs its own kind for two reasons: credit assignment (a record composed only of failures teaches avoidance, and nothing is repeatable from it) and, the mechanism it exists to exploit, that **knowing a thing has been done by someone at one's own capability level changes what is in the action space, which an instruction cannot do.** Held to the same evidence standard as a failure specimen: named actor, locatable primary, what would have happened instead; carries `:demonstrates <virtue>` so the class is gatherable. Live: comproprium `FORMAT.md` §D2. ✔ 7 live `:type demonstration` segments. | ch. 1 gap (segment kinds); ch. 9 [[pedagogy-layering]] |
| **N6** | vivarium's four **open** format questions, none settled and each naming what would settle it: does a specification need kinds a theory does not (a `requirement` kind + a non-propositional desideratum status axis is proposed and unadopted); does a segment need a `mechanism:` field — *"a declaration which cannot fail a build is a wish"*, with `~` meaning "none, and that is the point"; does it need a `sources:` field; does a promotion ladder built on a proof partial-order survive contact with a simulation platform. Live: `~/src/arch/vivarium/FORMAT.md` §Open questions. | ch. 1 gap; ch. 8; ch. 11 [[instance-profiles]] |

## Owed to Chapter 2 — order and views

| # | Residue | Owed to |
|---|---|---|
| **N7** | **The estate's most concrete cross-instance reference scheme, and nothing here carries it.** vivarium §5.2 defines one identity scheme for all Archema members — member namespace + local identity (`#asf/<volume>/<slug>`, `#asf/term/<slug>`, `#vivarium/<slug>`, `#logos/<paper>`), with a required prose form, an optional 1:1 wikilink alias, and the instruction not to invent a second dialect. Two rules make it work: **foreign prerequisites are not `depends:` rows** (tooling and promotion assume local files) but are cited in the body instead; and **verify a foreign slug exists before citing it**. Live: `~/src/arch/vivarium/FORMAT.md` §5.2. | ch. 2 estate-layer gap |
| **N8** | **A counter-instance to view-local metadata.** `asf/terminology` puts generated-view ordering data **on the record**: `seq:` (ascending, ahead of alphabetical) and `subgroup:`, where a globally monotonic seq space drives entry order *and* subgroup order together. That is view-order living on the atom, the opposite of [[view-edge-metadata]]'s position — worth stating as the case the claim has to survive, not omitting. Live: `~/src/arch/asf/terminology/README.md` §Generating LEXICON.md. | ch. 2 [[view-edge-metadata]] |
| **N9** | **A declared exposition-vs-dependency divergence, stated inline in the view itself.** udon-needs' outline carries: *"Ordering note: Part III (mutation) precedes this part by demand strength — it is the customer; addressing is what it consumes — but the build dependency runs the other way, and #priorities states it: addressing first, mutation on top of it."* A live worked instance of an accepted, visible ordering exception. Live: `~/src/arch/firmatum/udon/v2/udon-needs/02-tooling-needs/OUTLINE.md` Part IV (byte-identical twin under `~/src/MOVED/udon/`; firmatum is the live tree as of 2026-08-05). | ch. 2 [[dependency-order-tension]]; [[appendix-placement]] |
| **N10** | **Three named-absence genres from this chunk**, none carried: a **coverage ledger** as a sibling artifact (udon-needs `RESIDUALS.md`, "coverage ledger and bolt-on points"); an **honest-gaps-at-write-time section** on a project front door (behavioral-floor CLAUDE.md: "what an agent walking in cold should *not* assume is already done"); and comproprium's ruling that **empty is a finding, not a hole** — both trigger fields may legitimately be empty, and forcing one produces "a disposition wearing a trigger's clothes"; the weakest lines in its first harvest were the fabricated ones. | ch. 2 [[absence-as-structure]] |
| **N11** | **The star rows and the onboarding curriculum are the same data, and nothing says so.** [[view-genres]] already lists vivarium's ★ rows + `orient-rank` as an importance-annotation genre, and [[orientation-gate]] describes the gate without mentioning stars. The uncarried link is that **most of the gate's quiz mass is drawn from the starred rows** — so the importance annotation is simultaneously a view-local ranking and the onboarding curriculum, and a change to one silently changes the other. Live: `~/src/arch/vivarium/ORIENT.md`. | ch. 2 hotness gap; ch. 5 [[orientation-scaffold]] |  **PARTIALLY DISCHARGED 2026-08-06 → [[orientation-scaffold]]** (ch. 5 half — the curriculum/ranking coupling; ch. 2 view-local-metadata half still open)

## Owed to Chapter 3 — steady state, tracking, sidecars

| # | Residue | Owed to |
|---|---|---|
| **N12** | **Inheritance-by-reference is the estate's answer to per-instance FORMATs, and it is stated with its reason.** comproprium's FORMAT is *divergences only*: "The conventions here are **ported by reference**, not restated… Restating it here would fork it — an inlined definition is not a copy, and there is nothing holding two copies in step." It then names what is **carried unchanged** so nobody has to diff. vivarium states the same rule from the failure side: `ETHICS.md` restated the LEXICON by hand and thereby used a name the dictionary had retired four days earlier, invented two access channels that do not exist, and dropped two that do. Live: comproprium `FORMAT.md` §Carried unchanged; vivarium `FORMAT.md` §"Do not restate a definition that lives elsewhere". This is directly the shape ch. 11's generator would emit. | ch. 3 [[sidecar-conventions]]; ch. 11 [[derivation-not-templates]]; ch. 2 estate-layer gap |
| **N13** | **Format rules that bind every file, not only segments.** vivarium splits its FORMAT explicitly: §§1–4 govern claim segments; §§5–6 (cross-references, prose voice, math) govern *every file in the repository* — "*'It is only a working document' is not an exemption*, and the one time that exemption was taken, the result was a document that used a term the dictionary had retired four days earlier." | ch. 3 [[sidecar-conventions]]; ch. 12 |
| **N14** | **Two decision-record mechanisms this estate ships that nothing here carries.** (a) **A greppable precondition marker**: relata marks every decision resting on its private-repo assumption `[private-repo assumption]` precisely so a future "generalize for public sharing" sweep can find each load-bearing instance with one grep — and records that the one previously-marked decision was *resolved* rather than left marked. (b) **Deleting a refuted rationale outright**: relata's files-vs-sqlite trade study "was entangled with an unsound framing and has been **removed**", with an explicit anti-reconstruction instruction — *"do not inherit a prior conclusion or reconstruct the argument from memory; weigh it fresh when it is next worked"* — plus a neutral statement of current state so the deletion does not leave a vacuum. Note this is the same estate whose gen-1 and gen-2 stores still carry that trade study verbatim as a table; the deletion did not propagate. Live: `~/src/arch/firmatum/relata/README.md` §Status, §"Storage form — OPEN". | ch. 3 [[decision-records]], [[history-layer]] |
| **N15** | **A plan genre: the steward's own checklist quoted verbatim, with a dated agent reconciliation appended.** `~/src/neurips-reviews-responses/processing-flow.md` is Joseph's checkbox list reproduced as-is ("Here it is, as you typed it"), followed by a paragraph reconciling each block against the live tree and naming which boxes are *known-stale because written from a superseded identity*. Keeping the primary unedited and the reconciliation adjacent is a tracking pattern, not an accident. | ch. 3 [[tracking-altitudes]] |  **DISCHARGED 2026-08-06 → [[steward-surfaces]]**
| **N16** | **Counts are pointers, not numbers.** relata's README refuses to state its own corpus size as canon: it gives the commands that compute each figure and marks the dated snapshot "for orientation only." A norm about numbers in README-grade surfaces that the estate elsewhere violates freely. | ch. 3 [[history-layer]]; ch. 14 [[corpus-instruments]] |  **DISCHARGED 2026-08-06 → [[corpus-instruments]]**

## Owed to Chapter 4 — flux and integration

| # | Residue | Owed to |
|---|---|---|
| **N17** | **"Good math never lives only in spikes."** A derivation left in a spike is invisible to the theory — it cannot be cross-referenced, cannot be checked by the outline linter, does not appear in the outline, and is at risk when the spike is archived. So substantive math must land in an existing segment (if it tightens one) or a new appendix segment with its own claim identity and outline row; the spike keeps the attempt, the failed branches, and the reasoning trail. Live: `~/src/memorata/memory-curation/methodology/feedback_math_lives_in_segments.md`. This is [[integration-metabolism]]'s rule stated for one high-value content kind, with the reason given in terms of *what the corpus's instruments can see*. | ch. 4 [[integration-metabolism]]; ch. 3 [[working-note-lifecycle]] |
| **N18** | **Schema vocabulary may land before the tooling knows about it.** `bin/term decide` warns on an unknown action but does not block, "so new event-type vocabulary can land before the CLI knows about it." Paired with the same store's **permissive extractor / strict linter** split — `render` always emits and marks missing required fields `(missing)`, `lint` is where schema problems surface as findings — the effect is that the corpus can evolve its vocabulary without the build going down. | ch. 4 [[layer-speeds]]; ch. 11 [[generator]]; ch. 14 [[corpus-instruments]] |
| **N19** | **The resolution ladder as a general shape for "the system cannot decide alone."** relata degrades through five tiers, never silently: automatic → blocking interactive (on a TTY a rich prompt; **off a TTY, the same choices as JSON on stdout plus a non-success exit code**, so an agent decides and re-invokes) → batch choice list → a runnable pending report naming each item's stage, reason, and the exact command to decide it → the spool itself as the bottom tier, *not* the centre of the UX. Three acts kept distinct at any wait state: **rerun** (re-present the stored decision memo, no blind redo) ≠ **`--retry`** (recompute earlier stages, things may have changed) ≠ **decide**. And: *"a parked document is a question the system is asking,"* so ask it at the terminal while the context that makes it cheap to answer is still present. Live: relata README §1, §2. | ch. 4 [[influx-queues]] (beyond what it now carries); ch. 15 [[authority-routing]] |  **DISCHARGED 2026-08-06 → [[authority-routing]]**

## Owed to Chapter 5 — onboarding

| # | Residue | Owed to |
|---|---|---|
| **N20** | **The gate's incentive design, which [[orientation-gate]] does not carry.** vivarium's grading is graduated rather than binary — 5/5 unlimited commits for the session's compaction generation, 3–4/5 a **one-time** commit plus a promise to free-read the missed sections, <3 re-orient and retry; soft rejects (format errors, too few substantial answers) do not burn the attempt, real graded failures do. Two design consequences: **misses convert into targeted reading**, and **friction converts into a standing repair backlog** on a named claim segment, so the gate feeds the corpus instead of only guarding it. The post-pass norm names the first legitimate uses of write access as the repairs the free-read made obvious. | ch. 5 [[orientation-scaffold]] |  **DISCHARGED 2026-08-06 → [[orientation-scaffold]]**
| **N21** | **The quiz addressing is provenance addressing run forwards.** [[provenance-rot-specimen]]'s Working Notes already name "slug + section + word anchors, the address shape vivarium's orientation quiz already uses" as a candidate repair for rotting provenance. The addition owed elsewhere is the symmetry itself: one identity-anchored addressing scheme serves **retrospectively** (prove the quoted span is still there) and **prospectively** (prove you read it), so an instance that builds either gets the other nearly free. | ch. 5 [[orientation-scaffold]]; ch. 1 [[slug-identity]] |  **DISCHARGED 2026-08-06 → [[orientation-scaffold]]**
| **N22** | **The gate's costs, stated by its own operator manual:** one sealed test per checkout, so parallel agents on a single tree clobber each other; the seal is chmod-based with an emergency unseal; and "answerable from memory" pressure shapes which units are quotable at all. Bypass requires Joseph's approval or a reason you can own with him. | ch. 5 [[orientation-scaffold]]; ch. 13 [[partition-isolation]] |  **DISCHARGED 2026-08-06 → [[orientation-scaffold]]**
| **N23** | **Pre-action prescription blocks as a front-door genre**, appearing in two substrates independently: behavioral-floor's project CLAUDE.md carries `> [!warning]` blocks that fire at named moments ("before recommending wait for the next venue…", "before adjusting page budget — build the PDF and count"), and comproprium's praxes carry `|before-acting` / `|on-reading` triggers as segment fields. Same instrument, one as prose in an orientation file and one as a typed field on an atom — which is exactly the choice ch. 5 has to make. | ch. 5 [[role-activation]]; ch. 1 gap |  **PARTIALLY DISCHARGED 2026-08-06 → [[role-activation]]** (the two-shapes claim is carried; the typed-trigger-field specimen itself is cited at a stated remove, not re-verified — that verification is still owed)
| **N24** | **Landing obligations belong in the delegating brief.** The methodology note's corollary to N17: brief a spike agent with an explicit landing deliverable ("if novel math is derived, land it in segment X or create appendix segment Y with a new slug and an outline row"), and at review time verify the destination exists — "if it only lives in the spike, the work is not yet done." | ch. 5 [[work-coordination]] |  **DISCHARGED 2026-08-06 → [[work-coordination]]**

## Owed to Chapter 6 — releasing

| # | Residue | Owed to |
|---|---|---|
| **N25** | **An artifact can create obligations that outlive it, and they are tracked as records.** ✔ `~/src/neurips/adjudicated-common/reviewer-relationship-ledger.md` states its purpose as "who engaged how; collaborator candidates; **promises that revisions must honor**", carries a *Promises in rebuttals (revision must honor)* table under the rule *"If a rebuttal offered it, the revision carries it — or the promise is explicitly withdrawn with reason"*, and — the part worth copying — a companion section **"What was *not* promised (do not invent)"**, a named-absence store for obligations. Nothing in the outline treats obligations-created-by-publication as a record kind. | ch. 6 [[living-after-publication]]; ch. 2 [[absence-as-structure]] |
| **N26** | **A shared layer above sibling verisectoria.** `adjudicated-common/` sits above three per-paper adjudication trees and carries what is cross-cutting: voice and tactics, the truthification methodology, the backport queue, the reviewer ledger, a char-count snapshot. A worked instance of the estate layer at small scale. | ch. 6; ch. 2 estate-layer gap |

## Owed to Chapter 8 — truth and judgment

| # | Residue | Owed to |
|---|---|---|
| **N27** | **The concrete instrument behind a principle [[verification-provenance]] already states.** That segment's clause (C) carries the principle ("checking at the moment of writing accumulates silent lies afterwards"); relata supplies the mechanism and the cost, and neither is carried. The cost: **five entries spent two months asserting they held a PDF when the registered blob was an HTML login wall.** The mechanism: `relata audit`, a standing **non-destructive** recheck that verifies the *evidence* rather than the claims — every registered blob must still exist, still be a document **by content rather than extension**, and still hash to what was recorded — and that reports and hands you the command rather than writing. Live: `~/src/arch/firmatum/relata/README.md` §2c. | ch. 8 [[verification-provenance]] |
| **N28** | **`coverage:` — the field that stops a verifier over-reading its evidence.** A registered file may back only part of the cited work (sample chapter, extended abstract, differing preprint). Without the field, an agent checking a claim against the document would reasonably assume it holds the whole work. `full` is the default safe claim; the pipeline must never *silently* write `coverage: full` when a sample cue fires — it sets coverage honestly or escalates. | ch. 8 [[verification-provenance]] |
| **N29** | **Two temporal-truth mechanisms from vivarium, both with a recorded failure behind them.** (a) **A load-bearing measured number carries its kernel era** — emergent-statistic measurements are properties of the generator era that produced them, and crossing an era boundary requires a re-run or an era-scoped quote; a no-go result and a stability number each expired silently across such a boundary on the same day. (b) **A load-bearing citation carries its read-status** — material actually read is marked *read YYYY-MM-DD* and kept distinct from material named-not-read; a proposed term carve was retracted when its spike's sources turned out to be named rather than read. Live: `~/src/arch/vivarium/FORMAT.md` §5. | ch. 8 [[temporal-truth]]; [[verification-provenance]] |
| **N30** | **Verbatim material is not reformatted, for a mechanical reason.** comproprium keeps transport artifacts (ragged paste indentation, elision markers) *in*, and notes them rather than tidying, because a normalized span no longer matches its primary and the checker can no longer locate it: "preserve the artifact, and say in the segment that it is one. A reader can then discount it; a checker can still find it." Ruled after a harvest agent surfaced a case where faithfulness and readability genuinely pulled apart and declined to tidy it quietly. | ch. 8 [[verification-provenance]] |
| **N31** | **Two more strength ladders, both deliberately not truth-apt.** comproprium's `praxes` ladder grades **demonstrated firing** — `fired` / `fired-once` / `proposed` / `failed-to-fire` / `retired` — so a practice that is obviously correct and has never fired is `proposed`, "and that is the honest and useful label." Its `exempla` ladder grades **testimonial distance** — `attested` (first-person, by the one who lived it) / `reconstructed` (from a complete record, by someone who was not there) / `secondhand`, with the ceiling stated: nothing above `reconstructed` is available to an instance working from a transcript, however complete. [[strength-ladders]] owes an adjudication that these exist and are not rungs of the same scale as `exact`/`heuristic`. | ch. 8 [[strength-ladders]]; [[epistemic-axes]] |
| **N32** | **A standing counter-register as a first-class outline row.** udon-needs types one row `Counterposition`: "the standing register of evidence *against* our own theses — kept adjacent to the claims they qualify." A structural place for disconfirming evidence, in the view rather than in each segment's caveats. | ch. 8 [[evidence-ledgers]] |
| ~~N33~~ | *Retired 2026-08-05 — already carried.* [[evidence-ledgers]] item (E) states it: any number that gates a corpus belongs at exactly one site with a written justification of why it is that number and what would revise it. | — |
| **N34** | **A profile where the epistemic layer is one document, not a column.** causal-language keeps its epistemic discipline in a project-level `EPISTEMIC.md`; its own README describes that file as one that "openly catalogues where the work is weak, what would falsify it, and what survives" and directs auditors to engage it directly. ✔ Its eleven paper sections (`paper/src/00-…10-*.md`) carry no frontmatter at all except the metadata block in `00-meta.md` — so there is no per-section status anywhere to complement it. A live alternative to per-atom epistemic marking, and a candidate profile axis. *(Characterization is the README's, read live; `EPISTEMIC.md` itself was not opened during this pass.)* | ch. 8 [[epistemic-axes]]; ch. 11 [[instance-profiles]] |
| **N35** | **The separation test is running live in comproprium, including on itself.** Its directory split and its trigger-field split are both justified as "two labels earn separation where they route to different repairs", and its own open questions apply the test and report honestly that it *has not been run* on one of them. It also records a case where the test says split: the practices ladder cannot distinguish a probe firing for its author (evidence it is well-aimed) from one firing for an inheritor (evidence it *transmits*) — "and only the second is what a corpus is for." This is a live in-estate instance of R14's test with a fresh worked case. | ch. 8 [[epistemic-axes]] |

## Owed to Chapter 9 — pedagogy

| # | Residue | Owed to |
|---|---|---|
| **N36** | **A pass/fail criterion for the bridge kind.** udon-needs states it in one line: *"A bridge that merely summarizes its report has failed; a bridge succeeds when the reader arrives at the report ready to use it."* Its outline also carries `*Opens into:*` lines per part, naming which body reports each bridge hands the reader into — the bridge relation made explicit in the view. R25 established that three instances reached for the bridge kind; this supplies the criterion that would tell them apart from summaries. | ch. 9 gap (bridge/gloss kinds); ch. 2 [[view-edge-metadata]] |

## Owed to Chapters 11–14

| # | Residue | Owed to |
|---|---|---|
| **N37** | **The four-tier instance spectrum, as vocabulary.** full verisectorium (slug identity, separate outline view, typed claims with epistemic status, optional lint/build) · cousin store (dir-as-table, slug = file, append-only events, generated view, *no* pedagogical outline) · paper-section lite (one file per section, order in filename numbers, build concat, little or no status ladder) · pre-pattern. The tiering is the readiest-made input to the profile set, and it is what makes the family boundary arguable rather than assumed. (Overlaps `INFLUX/instances/deployments.md`, owned by another pass — check before landing twice.) | ch. 11 [[instance-profiles]] |
| **N38** | **Field-level projection, declared on the record rather than in the build.** [[derived-vs-authored]] now carries the clobber guard, the `Auto-generated` banner and the `.extracted.bib` naming convention; what it does not carry is the finer instrument in the same stores: `note:` is emitted to the published bibliography and `internal_note:` never is — two fields on one record differing only in *which projections may see them*. The split was introduced after agent working-metadata bled into the rendered bibliographies of three papers at once (recorded live in all three refs/relata READMEs, dated 2026-05-06). This is [[selection-and-projection]]'s projection operation at field granularity, and it is the one place in the estate where projection is declared on the atom instead of hardcoded in a build script. | ch. 12 [[derived-vs-authored]]; [[selection-and-projection]] |
| **N39** | **A measured false positive from an instrument reading a rule too widely.** vivarium's ledger has a `|ref` field whose declared purpose is to name files, commits and artifacts; an audit read its twenty `|ref` paths as violations of "references carry no path." They were the field working. The no-path rule governs *cross-references*, where a slug is an identity, not a field whose job is to point at where something lives. The inverse of taught blindness: an instrument taught a rule without its scope. | ch. 14 [[corpus-instruments]]; [[identities-over-locations]] boundary |  **DISCHARGED 2026-08-06 → [[corpus-instruments]]**

## Owed to Chapter 7 — and a dated provenance point

| # | Residue | Owed to |
|---|---|---|
| **N40** | **An early, Joseph-voiced statement of this project's founding claim — dated 2026-04-24, more than three months before the pattern was named.** From the collaboration-rhythm note: *"Future agents have 100% context turnover; an affordance that isn't surfaced might as well not exist"* — recorded as the reason Joseph asks architectural affordances to be surfaced at first-class level (proposals, tracking, memory) rather than left as implementation detail, because **the affordance is theory-of-the-project content**. The same note records that *"outline-as-view, segments evergreen"* was surfaced mid-session on 2026-04-24 and Joseph picked up the thread. Live: `~/src/memorata/memory-curation/collaboration/feedback_collaboration_rhythm.md`. Two uses: [[turnover-solution]] gains a dated prior statement of its own claim from the steward; the history layer gains a provenance date for the pattern's naming. | ch. 7 [[turnover-solution]]; CHANGELOG / history |

## Owed by the gather's own index

| # | Residue | Owed to |
|---|---|---|
| **N41** | **`INFLUX/00-INDEX.md` carries one thing that is not redundant with the notes it indexes: its "Not copied (left at live source)" list** — a deliberate record of what the 2026-08-05 gather chose *not* to bring in, so a later pass does not rediscover it as missing. Items: the full doc-store report (~3,500 lines; only the §12.2 extract was taken), the full `PROPOSALS.md` and architectural-proposals portfolios (extracts only), the live segment corpora themselves (too large), the `_self/PRACTICA.md` stub, the byte-identical twin of the 2026-07-23 generalization under `MOVED/udon/.../refine-more/`, and the `fmt-md/model/training/` tree snapshots. Everything else in that index — layout, reading orders, per-directory source tables — is either superseded by the provenance banners on the copies themselves or dies with the tree. **Adjudication:** the index should archive when the last INFLUX subtree empties, not before (it still describes live directories: `asf/`, `build/`, `synthesis/`, `tribunal/`, `vera/`, and `instances/deployments.md`). This row exists so the not-copied list survives that archiving. **Read it as frozen history, not as a map:** its Layout tree and relative links already point at files that have moved (`synthesis-missed-instances-2026-08-05.md` is in `.integrated/instances/`, `deployments.md` is still in `INFLUX/instances/`), which is ordinary for an archived index and worth stating so nobody follows them. | ch. 2 [[absence-as-structure]] |

---

## Working Notes

- Routed by OUTLINE row rather than by source note, so archiving the sources does not orphan the register. All source paths above are **live** trees, not INFLUX copies.
- The four most likely to change a decision rather than fill in a body: **N12** (inheritance-by-reference — the shape a generator would emit), **N14** (deleting a refuted rationale, and the anti-reconstruction instruction it needs), **N19** (the resolution ladder as the general shape for machine-cannot-decide), and **N40** (a dated statement of the founding claim in the steward's own voice, predating the project).
- **Overlap sweep run 2026-08-05** against all 48 segments then in `last-adhoc-src/`, by an independent pass. Result: **N33 retired** (carried by [[evidence-ledgers]]); **N11, N21, N27, N38 narrowed** to the part their neighbouring segments do not carry — in each case the *principle* was already drafted and the *mechanism, incident, or link* was not. No overlap found for the remaining rows. **N37** still overlaps material owned by the `deployments.md` pass and should be checked against it before landing.
- One row was **withdrawn** rather than narrowed: an earlier N10 sub-item treated "embeddings kept as a recorded negative instance" as an estate practice, but the phrasing traces to this gather's own synthesis note (`plan/.integrated/instances/synthesis-missed-instances-2026-08-05.md`), not to any live source — it is *this project's* bookkeeping, not an observed genre. Recorded here so it is not re-derived as a finding. The genuine estate specimens for do-not-resurrect stores are comproprium's `residue.md` and the neurips adjudicated `residue.md`, both already noted elsewhere.

---

## From `residue-vera-2026-08-05.md`

# INFLUX/vera — dispatch & residue register, 2026-08-05

*Metabolism pass over `plan/INFLUX/vera/` (25 files, four strands). Companion to `INFLUX-DISPATCH-2026-08-05.md`, which this file follows in form: a present-truth surface about **what is owed**, routed by OUTLINE row so later dispatches cannot orphan it. Retire a row when its content lands in a segment body or is expressly rejected.*

*Residue is numbered **V1–V24**, plus a late **V16b** (V for vera), to stay disjoint from the R-series in the earlier dispatch and from other parallel chunks. Numbers are identities, so V16b was inserted rather than renumbering.*

**Verification marks.** ✔ = verified first-hand during this pass, against live primaries, 2026-08-05. (agent) = read by a delegated sonnet agent whose full inventory is cited; I did not re-read those primaries end to end. (scout) = reported by an INFLUX synthesis and not independently checked.

**Two companion inventories** were produced by this pass and are part of it — they are the strand-level detail this register only routes:

- [`residue-vera-proprium-inventory-2026-08-05.md`](residue-vera-proprium-inventory-2026-08-05.md) — strand 1 (VERA the PROPRIUM component)
- [`residue-vera-ennaos-inventory-2026-08-05.md`](residue-vera-ennaos-inventory-2026-08-05.md) — strand 3 (the Nov 2025 ennaos VERA architecture)

---

## Posture: curation, and where that was right

The brief for this chunk set the expectation that yield would be **smaller in segments, larger in well-routed residue**, because Joseph said of this material: *"the vera stuff I haven't looked at in a long time... we'll need to go over that together."*

That held for **strands 1 and 3** (PROPRIUM VERA, ennaos VERA) and this pass treated them as inventory only — nothing extracted, both mapped for the joint session.

It did **not** hold for **strand 2** (comproprium). That strand is not old un-reviewed material: it is a **live, actively maintained corpus** that Joseph was working in five days ago, it is already the source of a drafted segment in this plan ([[provenance-rot-specimen]]), and it is itself a verisectorium instance with a reasoned FORMAT. Treating it as deferred would have been the wrong reading of a caution that was about something else. It produced the densest routed residue in this register and one drafted segment.

**Strand 4** (vox-vera) also turned out to be extractable and was not covered by the caution — it is external published research plus a design note, not a design of Joseph's awaiting review. It produced the other drafted segment.

---

## Dispatch record

**Every one of the 24 banner-carrying copies was verified byte-identical to its live original** (diffed modulo the provenance banner and its trailing blank line), and independently re-verified by an adversarial pass using its own stripping logic. The live trees are the citable landings; the copies are archive-shaped by construction. The remaining two files in the bin (`00-INDEX.md`, `synthesis-scatter-2026-08-05.md`) are gather-authored, not copies, and carry no banner.

**The moves below were executed** (`git mv`, 2026-08-05, after the verification pass finished reading the files): 22 copies to `.archive/vera/`, 2 gather artifacts to `.integrated/vera/`, and the ennaos pair deliberately left in `INFLUX/vera/`. Stated because an earlier draft of this table named these destinations *before* the moves fired — an intention written in the past tense, caught by the adversarial pass. If you are reading this and the files are not where the table says, the table is wrong and should be trusted less than the filesystem.

| Files | Destination | Why |
|---|---|---|
| `comproprium-segments/ver-*.udon` (12) · `comproprium-FORMAT.md` · `comproprium-README.md` | `.archive/vera/comproprium/` | ✔ all 14 byte-identical to `~/src/arch/proprium/comproprium/{vera/,FORMAT.md,README.md}`. **The live corpus is actively maintained and is the citable source.** Content routed to V1–V15 below. |
| `PROPRIUM-ONTOLOGY-v2.md` · `PROPRIUM-ARCHITECTURE-v2.md` · `asf-def-proprium-mapping.md` · `asf-terminology-proprium-mapping.md` | `.archive/vera/proprium/` | ✔ all four byte-identical to their live originals. Read in full by a delegated agent; the map that replaces them is the proprium inventory companion. Not ratified for extraction — V18–V20 carry what is owed. |
| `vox-vera/` (4 files) | `.archive/vera/vox-vera/` | ✔ all four byte-identical to `~/src/_core/zoetica/docs/refs/vox-vera/`. Content largely discharged into [[verbal-label-calibration]]; remainder is V16–V17. |
| `synthesis-scatter-2026-08-05.md` · `00-INDEX.md` | `.integrated/vera/` | Gather maps, role discharged. Their unique content — the four-strand collision table — is superseded by [[name-collision-across-stores]], which is *about* that table. |
| `ennaos-vera-architecture-final-specification.md` · `ennaos-gemini-chat.md` | **stays in INFLUX** | ✔ byte-identical, but **deliberately left**: this is the strand Joseph named for joint review, it is the largest un-metabolized design artifact in the set, and archiving it before that conversation would bury it a third time. The ennaos inventory companion is the reading surface; the files stay until the joint session adjudicates them. |

**Honest incompleteness.** `vox-vera/example-event-log-network.md` (28K worked example) and `vera_empirical_calibration.ex` (21K Elixir module) were skimmed for structure, not read line by line — they are implementation detail behind the calibration guide I did read fully, and nothing in this register rests on them. The ennaos pair was read by a delegated agent, not by me. Everything marked ✔ below, I ran or read myself.

---

## The four strands, and their review-readiness

The name collision this chunk exists to keep straight is now itself a drafted claim ([[name-collision-across-stores]]); this table is the operational version.

| Strand | What it is | State | Ready for what |
|---|---|---|---|
| **1. VERA**, PROPRIUM component | A named slot for belief-held-with-uncertainty in the ELI architecture | (agent) **A name and a one-paragraph sketch, not a designed subsystem.** No representation, no retraction protocol; "epistemic council" named and never defined | Joint review — and see V18, which says the OUTLINE row overstates this leg |
| **2. `vera/`**, comproprium | 12 precept segments in a live three-directory corpus with a reasoned FORMAT | ✔ Live, maintained, unchanged since gather | **Mined this pass.** V1–V15 |
| **3. VERA architecture**, ennaos | Nov 2025 neuro-symbolic epistemological specification + its design dialog | (agent) Substantial and internally honest about its own limits; abandoned in place, not superseded | **Joint review with Joseph — not extracted.** See the ennaos companion |
| **4. vox-vera**, zoetica | Verbal-probability calibration guide + Bayesian-KG design note + implementation | ✔ External published research (Mosteller & Youtz 1990; Budescu; Vogel 2022) plus in-estate design | **Mined this pass** → [[verbal-label-calibration]] |

**Did the live corpus move since 2026-08-05, as the brief expected?** ✔ **No.** `~/src/arch/proprium/comproprium/` has a clean working tree and its last commit touching it is `a40825f` (2026-08-01) — the same commit that broke the provenance paths. All 12 precepts are unchanged, and no new ones were added. The expectation that it would have moved was reasonable and is simply not what happened; the copies are current, not stale.

---

## Drafted this pass

Two segments in `last-adhoc-src/`. Proposed OUTLINE rows are in the report accompanying this file; the rows do not exist yet (OUTLINE is single-writer).

- **[[verbal-label-calibration]]** (ch. 8, `form`) — a strength label is a word, words are read with measurable spread, and no ladder in this estate has been calibrated. Deliberately **narrower** than the existing [[confidence-calibration]] row: it discharges only the vox-vera leg and leaves the VERA-lineage synthesis for the joint session.
- **[[name-collision-across-stores]]** (ch. 3, `emp`) — collision detection runs inside a store and is structurally blind between stores; the four-VERA situation is the specimen, and a terminology substore with bounded-context sharing is the instrument that would catch it.

**Not drafted, deliberately:** [[confidence-calibration]] itself. Its summary names four legs; two of them are the strands Joseph reserved. Drafting it now would pre-empt that conversation and would also, per V18, overstate one leg.

**A near-collision worth recording as process data:** [[write-semantics-declaration]] was on this pass's draft list from comproprium FORMAT D5, and a parallel agent was writing that exact file at the same moment. It was caught by listing `last-adhoc-src/` immediately before writing. This is [[lost-update-hazard]]'s shape at the file-creation grain — two agents creating the same new path, which git does not protect against any better than it protects a shared table. The residue below (V1) is what this pass would have contributed, routed to that segment instead.

---

## Residue register

### Strand 2 — comproprium (the dense one)

Live source throughout: `~/src/arch/proprium/comproprium/`. All items ✔ read first-hand from the live tree.

| # | Residue | Owed to |
|---|---|---|
| **V1** | ✔ **Write-semantics-per-type, with the reason and the exception stated.** `FORMAT.md` D5 does not merely have both semantics — it *argues* the split: *"a claim's job is to be presently true, an account's job is to be what happened."* And it names the exception, which is the part that makes it operational: a **factual error about the record** (wrong timestamp, misquote, misattribution) *is* corrected in place, "because the account was never attesting to them." So the rule is not append-only-versus-replace but **append-only about the interpretation, replace about the record** — a finer grain than the segment currently carries. Also supplies the segment's open Working Note on collisions-versus-siblings: comproprium's answer is the `:family` field (see V5). | [[write-semantics-declaration]] — already drafted by a parallel agent; this is the strengthening pass it should get |
| **V2** | ✔ **`:max-attainable` — a ceiling declared separately from the current status.** Every comproprium precept carries both `:status` (where the claim stands now) and `:max-attainable` (the highest tier it could *ever* reach). Eleven of twelve read `:max-attainable empirical`; **one, `ver-durability-wager`, declares `:status discussion-grade :max-attainable discussion-grade`** — a claim stating it can never be promoted, with the reason in its own body: *"no amount of further work turns this into a result. Recording that ceiling is more informative than leaving it open."* This distinguishes **not-yet-verified** from **not-verifiable-in-principle**, which no ladder in this project's outline currently can, and it makes "we should strengthen this" a checkable rather than a perennial suggestion. Genuinely novel among the mechanisms gathered so far. | ch. 8 [[epistemic-axes]] (a candidate axis, or a companion to strength); [[strength-ladders]]; ch. 8 [[temporal-truth]] (a ceiling is the thing revisit-criteria should respect) |
| **V3** | ✔ **Three strength ladders inside one corpus, deliberately.** `FORMAT.md` D4: `vera/` ports the ASF claim ladder unchanged; `praxes/` uses `fired · fired-once · proposed · failed-to-fire · retired`; `exempla/` uses `attested · reconstructed · secondhand`. The argument is explicit — the ASF ladder "is calibrated for claims" and a practice is adjudicated by *whether it fired*, not by whether it is true. Note the `praxes` ladder is **not a strength ladder at all**: it measures demonstrated firing, and "a practice that is obviously correct and has never fired is `proposed`, and that is the honest and useful label." [[strength-ladders]] owes an adjudication over "at least three incompatible ladders"; here are three more, in one corpus, chosen on principle — which is evidence that the adjudication's answer is *not* a superset ladder. | ch. 8 [[strength-ladders]] — materially changes what that row can conclude |
| **V4** | ✔ **Type vocabulary is local at a finer grain than "per instance."** `FORMAT.md` D2 declares three separate type vocabularies, one per directory, within a single corpus (`principle/mechanism/observation/definition/hypothesis/discussion` · `probe/mitigation/practice/protocol` · `account/exchange/testimony/quotation/demonstration`). [[type-vocabulary-locality]] currently locates the locality at the instance boundary; this says the real grain is the **adjudication instrument**, and one instance may hold several. Also carries a portability rule worth stealing: *"Where a claim would be well-typed in ASF's vocabulary, use ASF's word instead, so it can travel."* | [[type-vocabulary-locality]] |
| **V5** | ✔ **`:family` — restatements linked, never deduped.** *"Two segments are duplicates only when they resolve to the same span in the same primary. The same idea said differently, to a different audience, at a different time is a restatement: both are kept, linked by `:family`"* — with the failure named: *"Deduping on sentiment rather than span flattens a family into whichever version an early agent preferred."* This is a **direct hit on a `--GAP--` row** (ch. 1: restatements, families, aliases, same-claim-different-expression links) — the gap has a shipped answer in the estate, with a stated identity criterion. | ch. 1 gap row on restatements/families — arguably promotes it to a real row |
| **V6** | ✔ **"An outline may reference and order. It may not introduce."** From `README.md`, with the reason: *"The alternative is an outline asserting things at a granularity nobody adjudicated."* And beside it: *"a segment's \*type\* is intrinsic, its \*position\* belongs to a view"* (emphasis in the original), plus the corollary *"Material unique to one view is still a segment, even if it is one line, referenced by exactly one outline."* This is an independent restatement of this project's central thesis, from a corpus that is not this project — the "one-line segment" corollary in particular is a sharp operational test that nothing in the outline currently states. | ch. 2 [[outline-as-organizing-principle]]; [[view-edge-metadata]] |
| **V7** | ✔ **`ver-silent-absence-reads-as-success` — the collision asymmetry, from the absence side, with eight specimens.** Its formulation: *"A wrong answer is a claim, and a claim can collide with another claim — the mechanism the whole segment arrangement rests on. **An absent answer makes no claim, so it can collide with nothing**, and no amount of downstream discipline recovers it."* Eight specimens in one evening, five found independently by three agents not looking for it (exit-code collapse, an `nargs='+'` swallowing a query, a multibyte delimiter, `grep \| head` always exiting 0, an absence claim written as searched when the search had not run, …). Two repairs named, pointing different directions: **tools** must distinguish empty/failed/unmatched; **claims** must name the search that produced them. This is both a third specimen for [[collision-staleness-detection]] and the mechanism account [[absence-as-structure]] is missing — that row says absence handling "is what actually rots estate-wide" and R2 measured it, but neither says *why* absence is structurally worse than error. This does. | ch. 2 [[absence-as-structure]] (strongest single item it could stand on); [[collision-staleness-detection]] (third specimen, from the negative side — compare R20) |
| **V8** | ✔ **`ver-placement-over-quality` — a candidate answer to the ch. 7 gap.** *"Not one failure recorded here was a knowledge gap. In each case the correct principle was present — in the agent's own words, in its context, often minutes earlier — and the failure occurred anyway."* Therefore *"the operative variable is not how well a thing is understood but **where what is already understood has been put**."* Same-day support from a second instance: three precepts sat **verbatim in a successor's system prompt and were each violated at the moment they named**, while structural interventions built the same evening held without cooperation. The ch. 7 gap row asks *"why it works even applied ad-hoc and unevenly — the latent-need account; which properties carry most of the effect."* Placement-over-quality is a direct, independently-authored candidate answer: the pattern's effect would come from *placement of known things*, not from improving knowledge. The author's own caution is kept and is load-bearing: it may be an artifact of what a day of *orientation* failures looks like; a day of *reasoning* failures would plausibly generalize content-shaped. Held `discussion-grade`, `:max-attainable empirical`, with the test named (keep collecting; check whether new findings are also locational). | ch. 7 gap (which properties carry the effect) — the most decision-changing item in this chunk |
| **V9** | ✔ **`ver-manufacture-originals` — why chaining works, and a corpus-scale specimen.** *"A defect introduced by editing is detectable, because the pre-edit state exists... A defect present in a first writing has nothing to be compared against."* Hence: *"the least-checkable material in any corpus is fresh outlines, new convention files, and syntheses — which is most of what agents produce, and precisely what corpora get built out of."* The move is to **create** the comparison rather than look harder. It explains D5's append-only chaining as *manufacturing the original that makes the correction checkable* — which is the mechanism V1 is missing. Corpus-scale specimen: quotation segments extracted programmatically and verified back against the primary found four defect classes in one evening (line-wrapping inside a quote, collapsed interior whitespace, a hyphenated word split across lines, a substituted quote character), **none of which would have survived proofreading, because none changes how the text reads.** | [[write-semantics-declaration]]; ch. 8 [[verification-provenance]]; ch. 1 [[atom-as-cluster]] |
| **V10** | ✔ **`ver-act-not-intention` — interventions must fire at an act, not a disposition.** *"A disposition-shaped intervention... has no moment to attach to, so it attaches to all of them equally, which is to say none."* Negative specimen: six checks written as stances fired on schedule all morning and returned clean while the agent produced exactly what they watched for; one *did* fire, was read, and was answered *"yes, but it is warranted."* Rewritten as moments-with-acts, the same list returned three real findings within a minute. Bears directly on how orientation gates and state flags are written — a gate phrased as a disposition is decoration. Note the corpus measured this **on the author of the segment itself** (`ver-manufacture-originals` working notes: the same defect class three times in one evening by an instance already corrected twice; the checker caught every instance in under a second). | ch. 5 [[orientation-gate]] / [[priming-discipline]]; ch. 4 [[gates-need-destinations]] |
| **V11** | ✔ **Triggers split into two fields, and empty is a finding.** `FORMAT.md` D3a: `|should-come-to-mind-when` was one field doing three jobs; split into `|before-acting` (a pending act) and `|on-reading` (an input pattern). The observation that made the split: on-reading triggers *"fire on reading rather than on acting, which makes them arguably more reliable than the act form: they match incoming context in the same pass that reads it, with no pending action to introspect. Nothing in the corpus had noticed they were a different mechanism."* And: *"Both may be legitimately empty, and empty is a finding rather than a hole"* — forcing a trigger produces "a disposition wearing a trigger's clothes." Found by an agent that wrote 32 segments against the old field **and reported the strain rather than absorbing it**, which is itself a process datum worth keeping. | ch. 2 [[absence-as-structure]] (empty-as-finding); ch. 1 inner-section gap |
| **V12** | ✔ **The separation test, applied twice more.** `FORMAT.md` D1 justifies the three-directory split by *"source §9's own test for when two labels earn separation: they route to different repairs,"* and the Open section runs it again on a live case — the `praxes` ladder *"does not distinguish fired-for-its-author from fired-for-an-inheritor, and they are different events... only the second is what a corpus is for. Two repairs, so by source §9's own test this probably earns separation."* R14 already registers the test; these are two further worked instances, one of them a *live undecided case* rather than a retrospective one, which makes it the better teaching example. | ch. 8 [[epistemic-axes]] / [[strength-ladders]]; strengthens R14 |
| **V13** | ✔ **The checker reports named absence as its own category.** `bin/check-corpus` (re-run this pass) prints, beside pass/fail: **`5 forward references — expected per FORMAT source §7.2, each names a segment that should exist`**, listing them. So a reference to a not-yet-written segment is neither a pass nor a failure but a third, named, counted state. This is the **positive** counterpart to R4 ("the estate writes absence law it does not run") — here is absence law that *is* run, in a shipped instrument, and it is the smallest working example of the discipline [[absence-as-structure]] argues for. Its summary line also prints the denominator (`3/109 quoted spans located`) rather than only failures — the practice `ver-silent-absence-reads-as-success` names as its own repair. | ch. 2 [[absence-as-structure]]; ch. 14 [[corpus-instruments]] |
| **V14** | ✔ **A steward's priorities recorded as *his view*, with the corpus's own failure noted beside them.** `README.md` §Priorities gives Joseph's ordering *"laid on the table as his view — not issued as an ordering to adopt,"* and then names the defect it is correcting: *"An earlier version of this section presented them as the ordering. That was the corpus's own #9 — a steward's shared thinking rewritten in canon voice — committed while writing the segment about it."* A worked instance of the steward-interface problem: how a steward's weight gets recorded without becoming law, including the failure mode and a live correction. Related and reusable: priority 5 declines to filter the harvest for prompt-worthiness because *"Salience and what gets promoted are settled later, empirically... not by anyone's judgment now,"* which is an explicit anti-premature-ranking rule. | ch. 15 [[steward-surfaces]] / [[authority-routing]]; ch. 3 [[warrant-over-authority]] |  **DISCHARGED 2026-08-06 → [[steward-surfaces]]**
| **V15** | ✔ **"The corpus on disk is the corpus" — deliberate untracked members.** `README.md`: a few segments are intentionally not in git, listed with reasons in `.gitignore`, and *"They are real members — indexed, referenced by outlines, verified by the checker — and simply not committed."* Stated explicitly *because otherwise the absence is silent, and a later completeness audit would conclude those segments never existed.* A concrete case where the store boundary and the version-control boundary deliberately differ, with the audit hazard pre-empted in prose. | ch. 2 [[absence-as-structure]]; ch. 13 gap (what the git layer does and does not catch); ch. 14 [[corpus-instruments]] |

### Strand 4 — vox-vera (mostly discharged)

| # | Residue | Owed to |
|---|---|---|
| **V16** | ✔ **The machinery deliberately left behind.** [[verbal-label-calibration]] carries the calibration argument; it does **not** carry Beta-distribution encoding of verbal phrases (method-of-moments from median + IQR), credible-interval computation, conjugate Bayesian updating from pseudo-observations, or CPT construction over a claim DAG. These are specified in working detail at `~/src/_core/zoetica/docs/refs/vox-vera/` (guide + `vera_empirical_calibration.ex` + a 28K worked example). Left out because they belong to a machine-inference design this project has no use for **yet** — but the ch. 8 gap row asks for exactly "Bayesian propagation across the dependency DAG (how a changed premise re-prices its dependents)," and this is a ready-made, already-written answer sitting one directory away. | ch. 8 gap (Bayesian propagation) — do not re-derive; it exists |
| **V16b** | ✔ **The estate already consumes this research — in ASF's theory, not in its formats.** `~/src/arch/asf/03-llm-core/OUTLINE.md` carries a detailed account of the embedding-geometry results (ρ figures, cross-validation against Vogel and Wintle, 5 model families, 8 languages) and reads them as *"empirical substrate for the claim that logogenic agents can leverage language-geometric encodings of epistemic states for $U_M$ / $U_o$ / hedging-projection estimates rather than relying on numerical instrumentation."* So the correct framing is **not** that calibration research is unknown here — it is that it is applied to *how an agent estimates its own uncertainty* and never to *the tier words the corpora mark claims with*. Worth knowing before anyone repeats the stronger, wrong version. (This pass initially wrote the wrong version, from a truncated grep; corrected before landing. The near-miss is itself an instance of V7.) | ch. 8 [[confidence-calibration]]; ch. 7 [[first-principles-grounding]] (a live AAT-side connection the plan does not know it has) |
| **V17** | ✔ **Presentation calibration and decision calibration are different layers.** vox-vera calibrates *how a confidence word is received*; relata's machinery calibrates *how independent evidence combines into a strength at all*. Both were filed under "VERA" in the gather and the distinction is easy to lose. [[verbal-label-calibration]] states it in a Working Note; it deserves to be stated wherever ch. 8 is finally organized, because conflating them makes "calibration" look like one problem with one answer. (The framing is drawn from `.integrated/synthesis/relata-methods-for-verisectorium-2026-08-05.md` §6.2 — dispatched there by an earlier pass — and checked against the vox-vera primaries here.) | ch. 8 organization generally; [[evidence-ledgers]] |

### Strand 1 — PROPRIUM VERA (inventory only; detail in the proprium companion)

| # | Residue | Owed to |
|---|---|---|
| **V18** | (agent) ✔ **The OUTLINE row overstates this leg, and should be reworded.** [[confidence-calibration]]'s summary names "the VERA lineage (PROPRIUM qualified truths, ennaos architecture research, vox-vera verbal probabilities, comproprium precepts)" as though four comparable bodies of work stand behind it. Per the delegated full read of both v2 documents, the PROPRIUM leg is **a named slot with a one-paragraph mechanism sketch**: two-tier acquisition, a weekly re-audit *cadence label with no procedure attached*, and otherwise placement in generic tables and passing mentions rather than any dedicated treatment — no representation for uncertainty, no retraction or propagation protocol, and an "epistemic council" named but never defined. I did not re-read the 1,565 lines myself, so the *count* is (agent); the consequence is not — a row summary that implies substance which the primaries do not carry is a truth-status defect in the outline, and this row is currently carrying one. | [[confidence-calibration]] row summary — a lead-session edit, before anyone drafts against it |
| **V19** | ✔ **A live, currently-broken second provenance-rot specimen — verified myself.** `~/src/arch/asf/terminology/entries/proprium-mapping.md`, a canonical terminology entry, points readers at `~/src/firmatum/PROPRIUM-ONTOLOGY-v2.md` and `~/src/firmatum/PROPRIUM-ARCHITECTURE-v2.md`. **Neither exists; `~/src/firmatum/` does not exist**, the tree having moved under `~/src/arch/`. The same file's ASF-internal reference is identity-shaped (`#def-proprium-mapping`) and resolves. One file, two reference designs, opposite outcomes — an independent replication of [[provenance-rot-specimen]] in a different corpus, from a different move, still broken today. [[identities-over-locations]]'s own Working Note predicted these would be cheap to find; this one was found incidentally. Carried in [[name-collision-across-stores]] §Strength, and it also wants a one-line fix. | [[identities-over-locations]] / [[provenance-rot-specimen]] (replication); ch. 14 [[corpus-instruments]] — asf's link checker does not check prose paths into other trees |
| **V20** | (agent) **Duplicate copies of both v2 documents exist under `~/src/arch/proprium/INGEST/old-firmatum/`, not diffed against the canonical pair.** A live collision surface if either is edited — the same shape as R22's twin-homes finding. Also noted: a v1 archaeology layer under `canonical/../archaeology/`, and draft-stage bridge files under `INGEST/msc-from-harness/bridges/` that appear to be earlier drafts of the two small ASF mapping files. None of this was resolved by this pass; the point is only that "the canonical pair" is not the only copy in flight. | ch. 3 [[history-layer]] / housekeeping; the joint session |
| **V23** | (agent) **A calibration-failure taxonomy already exists in the architecture document, and it is instrument-shaped.** Architecture v2 §8's failure-mode table gives three graded miscalibration modes with named observables: *gain too high* → beliefs churn, diagnostic **"track VERA entry churn rate"**; *gain too low* → stale beliefs despite contradicting evidence, diagnostic **track prediction accuracy over time**; *gain collapse* → confident-wrongness, diagnostic **calibration score on prediction-outcome pairs**. These are general model-level diagnostics rather than VERA-specific design, which is exactly what makes them portable — they are three concrete, countable instruments for "is this corpus's confidence marking any good," and this project currently has none. Note the shape: each is a *rate or a score over time*, not a state flag. | ch. 14 [[corpus-instruments]] / [[observation-stores]]; ch. 8 [[temporal-truth]] |
| **V24** | (agent) **Epistemic grading applied at prose-section grain — a coarser-grained ancestor worth having as a contrast case.** Both v2 documents grade their own content with a Level 1 / Level 2 / Level 3 scheme (functional-need / structural-constraint / mechanistic-hypothesis) applied to **whole sections of prose**, not to atomic independently-identified claims. The reading agent's judgment, which seems right: it is "a coarser-grained ancestor of the discipline you're studying, not an instance of it." That makes it a useful negative data point for [[atom-grain-parallelism]] — the same epistemic instinct, at a grain too coarse to let a claim be independently cited, superseded, or delegated. Also worth knowing: the small ASF mapping file derived *from* these documents does carry `depends:` frontmatter, so the grain change happened exactly at the crossing into the segment corpus. | ch. 1 [[atom-grain-parallelism]]; ch. 7 gap (which properties carry the effect) |

### Strand 3 — ennaos VERA (reserved; detail in the ennaos companion)

**Not extracted, by design.** These are the two items a reader of this register should know exist; everything else is in the companion.

| # | Residue | Owed to |
|---|---|---|
| **V21** | (agent) **`epistemic_status` as an axis orthogonal to the numeric confidence value** — substrate-generated vs. tribunal-validated vs. empirically-tested — i.e. "plausibility vs. validated truth" made a first-class field rather than a tier on the strength ladder. This is the same axis-collapse repair R14's separation test keeps finding, arrived at independently in Nov 2025, and it is close kin to V2's `:max-attainable`. Worth putting the three side by side at the joint session. | ch. 8 [[epistemic-axes]]; the joint session |
| **V22** | (agent) **An idea dropped rather than superseded:** inter-ELI epistemic graph-merging with trust-discounted confidence import, sandboxed with rollback, appears in the design dialog and is absent from the final specification with nothing arguing against it. Flagged by the reading agent as "dropped, status unclear." Exactly the kind of thing a joint review exists to adjudicate, and exactly the kind of thing that is lost when design dialog is not metabolized. | the joint session |

---

## For the go-over-together session

An agenda this register can be run from directly, in the order that seems most useful:

1. **The row that is wrong now** — V18. [[confidence-calibration]]'s summary implies four comparable legs; one is a name with a sketch. Reword before anyone drafts against it. Ten minutes, and it prevents a segment being built on air.
2. **Strand 3 is the real conversation** — the ennaos specification is substantial, internally honest about what it does not solve, and abandoned in place rather than superseded. The companion inventory is the reading surface. V22 (the dropped merging idea) is the one item nothing argues against.
3. **Three independent takes on one repair, side by side** — V2 (`:max-attainable`), V21 (`epistemic_status` orthogonal to confidence), and the estate's existing strength ladders. Three different corpora, three years apart, all separating *how strongly held* from *what kind of thing could ever hold it*. If that converges it is a finding; if it is inheritance it is worth knowing which.
4. **V8, which may be about the whole project** — placement-over-quality proposes that the pattern's effect comes from where known things sit, not from knowing better. That is a candidate answer to ch. 7's open gap and it was authored outside this project.
5. **What "VERA" should mean going forward** — four live referents, one name, no instrument that can report it ([[name-collision-across-stores]]). This is a naming decision only Joseph can make, and it is cheap now and expensive later.

---

## Verification

Two adversarial sonnet passes, both told to try to break this work rather than confirm it. Full verdicts: [`verify-vera-residue-2026-08-05.md`](verify-vera-residue-2026-08-05.md) and [`verify-vera-segments-2026-08-05.md`](verify-vera-segments-2026-08-05.md). Everything they caught was fixed before this file landed.

**Held under independent re-checking:** all 24 byte-identity claims (re-diffed with the verifier's own banner-stripper rather than mine); every quotation in V1–V15 located verbatim in the live comproprium files; the commit hash and clean-tree claim; the exact `check-corpus` output including the five named forward references; the 11-empirical/1-discussion-grade `:max-attainable` split; the dead `~/src/firmatum/` paths and the surviving identity-shaped reference beside them; the duplicate copies under `old-firmatum/`; strand separation across all four referents in every file; and the two companion inventories' findings against my one-line summaries of them, with no confidence upgraded in the retelling.

**Caught and fixed — worth reading, because the defects are the instructive part:**

1. **The dispatch table described moves that had not fired.** I deferred the `git mv` so as not to pull files out from under a verifier that was reading them, then wrote the destination table in the settled past tense anyway. The content claims were all true; the claim that the filing had happened was false. Fixed by running the moves. This is the register's own bookkeeping committing the exact error its `influx-not-warrant` rule exists to prevent, one level up.
2. **A false summary built on a truncated search.** A draft of [[verbal-label-calibration]] claimed every hit of a calibration-research grep fell into three named buckets. It did not — the grep had been read through `head -20`. A truncated search and an exhaustive one produce identically-shaped output, which is V7 exactly, committed while writing the register that carries V7.
3. **An overclaim past its own caveat.** The same segment tiered its weakest leg honestly in one section while its title, summary line, and main claim asserted that leg as settled — the hedge sat *after* the sentences that needed it. Rewritten so the untested step is named where it is first relied on.
4. Three small factual corrections: 53 not 54 Mosteller expressions; six models spanning five architectural families, not "5 models"; and Mosteller is the embeddings axis's *training* signal, with Vogel and Wintle the held-out confirmation — a distinction that changes what the convergence argument is entitled to claim.
5. One stale citation path and one silently dropped pair of emphasis markers in a quotation.

## Handoff — two things this pass deliberately did not do

- **`INFLUX/00-INDEX.md` still lists the departed `vera/` rows.** The earlier dispatch struck its own rows there; this pass did not, because that file is being read and written by four concurrent chunk agents and a read-modify-write on it is precisely [[lost-update-hazard]]. It wants one single-writer pass once the chunks land, not four racing edits.
- **The `git mv` renames were swept into a neighbouring agent's commit** (`302cba1`, whose message is about a different chunk entirely) before this pass could commit them itself. Nothing was lost — the move is complete and consistent in HEAD — but the provenance is now smeared across an unrelated commit message. A live, harmless instance of the concurrency the plan theorizes about; recorded rather than tidied.

## Working Notes

- Routed by OUTLINE row rather than by source file, following the earlier dispatch's convention, so that dispatching the rest of INFLUX cannot orphan these.
- The four items most likely to **change a decision** rather than fill in a body: **V8** (placement over quality — bears on ch. 7's core gap), **V2** (`:max-attainable` — a mechanism no ladder here has), **V7** (absence makes no claim, so it collides with nothing — the mechanism [[absence-as-structure]] lacks), and **V18** (an OUTLINE row that currently overstates its evidence).
- ✔ **[[provenance-rot-specimen]]'s counts are still current**, re-run this pass: `57 segments · 3/109 quoted spans located · 106 fail · 0 warn · 5 forward refs`. The breakage is **still unrepaired four days on**, and none of the three repair options in that segment's Working Notes has been chosen. That the numbers have not moved is itself worth knowing — the segment does not need a re-dating, and the decision it flags as precedent-setting is still open.
- Strand 2's live corpus is where new comproprium precepts should be written. Nothing in this project should edit the archived copies, and nothing should cite them.
- Not examined by this pass, and not claimed about: everything outside `INFLUX/vera/`.
