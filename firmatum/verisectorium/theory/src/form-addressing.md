---
slug: form-addressing
form: formulation
type-expected: formulation
status: proposed
max: decided
state: [pre-drafted]
depends: [def-atom]
---

# Formulation: Layout-Independent Addressing

Slug + section + word-anchor spans for provenance, quizzes, and cross-refs alike; designator resolution as a ladder (auto → interactive → batch → pending → spool), never silent best-guess.

## Working Notes

*(pre-draft: caution + coord notes, 2026-08-09)*

- **Misfire-feedback caution carried from the outline:** section-vs-frontmatter epistemics is a *principled disagreement*, not just a spans question — where epistemic state lives changes what an address into an atom can promise.
- Live development since the row: the udon paths instance (deployment #2) is now the working ground for the whole addressing question — its `def/` foundation (reference-act, binding/match, scope, resolution-with-paths) plus ESOP15's scope-graph machinery are the primaries; the steward's own call (2026-08-06) routed local design to udon paths rather than solving it here. This segment, when drafted, mostly *imports* that instance's settled terms and adds the verisectorium-specific demand (atom/part/span addressing for provenance and quizzes).
- The four-persistence-contracts cut (identity/description/content/moment) from the archived problem statement survives in the paths instance's live work — draft from there, not from the archive.
