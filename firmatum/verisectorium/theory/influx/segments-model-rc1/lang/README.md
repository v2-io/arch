# lang/ — the first executable cut of 09-LANGUAGE

*Register: spike / ground-validation. "vsl" is a working name owing a naming pass. `python3 vsl.py specimen.vsl` re-runs everything; `RUN-LOG.txt` is one captured run.*

- **`vsl.py`** — parser + semantics, stdlib only: declarations (dimension with rungs/channels/refutes/ceiling/family/strong-at · kind · record · rule · era), acts (event, era-bump), checkpoints (reads), computed projections (per-dimension position, strongest-line, floor, shown-independent-agreement with wrapper look-through), guards (rules gate lifts), falsifier-reopen with what-changed, era decay, and the three refusals: **fusion** (unrolled-permutation ladders rejected), **substitution** (no syntax places one dimension's value on another), **hand-set status** (a parse error by design).
- **`specimen.vsl`** — both SPECIMEN.md records stated in the language; the run reproduces the specimen's projections at every checkpoint, including agreement shown → era-bump lapse → committed-re-run shown again, the guard refusing premature ratification, and the falsifier firing a principled reopen. Order-invariance of recording verified by post-hoc trail shuffle (economizing guards stay order-sensitive — the model's carve, on purpose).
- **`demo-*.vsl`** — the three expected-failure articles.

What this run *is*, in the model's own terms: a `built → convicted-passing` event on 09's design — the sketch's central commitments (stating-is-recording is reachable; the laws become lint; hand-set status inexpressible) survived contact with an implementation and two specimens. What it is *not*: the language (syntax undecided, udon integration unexplored, one afternoon's spike). Next test articles per 09: the udon reference defs.
