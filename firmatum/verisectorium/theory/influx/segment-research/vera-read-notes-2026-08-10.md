# Vera strands — coord reading notes (2026-08-10)

*All four `theory/influx/vera/` files read whole after the reservation's scope was corrected (adjudication-only, never a reading prohibition). These are reading notes for the taxonomy work and the joint session — NOT adjudication, which remains Joseph+lead. Register caution binds in full: the ennaos material is design-dialog (Nov 2025, authors Joseph + Claude + Gemini), never implemented or measured; it "sounds more settled than it is."*

## The headline for the taxonomy work

The ennaos VERA spec is **the estate's own strongest pre-figuration of the five-track architecture**, designed nine months before the external survey — same-estate coherence (not corroboration), but under genuinely different pressure (ELI consciousness infrastructure, not corpus maintenance). One artifact independently carries:

- **`claim_type` ⊥ `epistemic_status`** — stated as "orthogonal to type" in the struct itself: kind-gates-state, in code shape, Nov 2025.
- **Staleness as resettable flags with reasons** (`stale`, `stale_since`, `stale_reason: dependency_changed | cycle_update | transitive`) — flags-not-gates plus *referent-coupled freshness* (a claim goes stale because its supporters changed), designed before either was named here.
- **Confidence as Beta distributions with credal-set escalation** for deep uncertainty — the interval-width/evidential-weight design; Walley 1991 is literally in its reference list.
- **`epistemic_status: tribunal_validated` as an event-gated rung** (L8's shape) and **"the graph records the results of reasoning, it does not perform the reasoning"** — L5's status-is-a-projection-of-adjudication, as the architecture's single stated core principle.
- **Three typed edges** (SUPPORTS / CONTRADICTS / SYNTHESIZES) with rejected alternatives *recorded with reasons* (generic-RELATED rejected for semantic loss; many-edge-types rejected as over-specification) — a worked precedent for our typed-`depends:` discussion, including a deliberate minimality argument to weigh against SKOS/AIF's richer families.
- **Per-edge `temporal_decay_factor` and `evidence_grade`** — freshness and grade at *edge* grain.
- **A triage economics formula** (`epistemic_value = significance × uncertainty × log(cascade_size) × staleness_age`, priority = value/cost) — EVI-adjacent gap-ordering, sketched in Part VII.
- **Developmental stages parameterizing thresholds** (nascent→mature changing decay half-lives, oversight, tolerance) — enforcement-profile-by-maturity, an axis our kit questionnaire wants.
- **Adversarial detection** (gaslighting, epistemic DDoS, coordinated deception, echo-chamber) — membrane hazards no other estate artifact names.
- **Metacognition queries** (`uncertainties()`, `circular_reasoning_check()`, `cascade_awareness()`) — the de-novo-auditor criterion as an API.

## The chat's distinct contributions (beyond the spec)

- **The ledger-not-truth-computer pivot's actual origin**: Gemini's honest-assessment turn (intractability of credal propagation; GOFAI trap), answered by Claude's layered synthesis (discovery=graph / reasoning=tribunal / recording=graph / propagation=hybrid). The exchange is itself a specimen of adversarial correction improving architecture — and of Joseph explicitly requesting "strictly honest thoughts… on the advisability of continuing" (the steward soliciting the refutation of his own program's direction).
- **Typology of tensions** with structural clues (context mismatch ↔ high semantic distance between support bases; boundary-condition ↔ threshold-crossing conflicts; flawed foundation ↔ high betweenness centrality) — already summarized in [[claim-dialectical-synthesis]]'s outline row; the primary adds the *graph-statistical* discriminators.
- **EMR (Epistemic Merge Request)** mechanics: trust-as-claim, trust-modulated import (scale Beta α,β by k), sandboxed integration with rollback, congruence/persuasion outcomes (reinforcement / productive conflict / destabilization) — the [[disc-epistemic-merge]] substrate, far-future by that stub's own note.
- The **name "EJG"** (Epistemic Justification Graph) was coined here by Gemini; "VERA" remained.

## For the joint session (unchanged reservation)

The comparison set is now fully readable on the lead's side: ennaos VERA spec ↔ comproprium's verum kind (FORMAT D2/D4) ↔ relata's shipped WoE ledger ↔ the REASSEMBLED-MODEL's five tracks. Candidate central question for the session, offered not settled: the spec's single-node-type decision ("everything is a Claim," differentiated by properties) is the *opposite* pole from our eleven-family carve — reconciling "one kind, many properties" vs "many kinds, gated axes" may be the session's deepest fork, and the spec records its reasons (type-boundary problems, uniform querying) which deserve to be argued against the survey's L1 rather than dismissed.
