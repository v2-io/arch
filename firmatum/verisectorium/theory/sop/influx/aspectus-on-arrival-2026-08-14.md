# Praxis candidate: aspectus on arrival — blindness must be owned

*Participant feedback + steward proposal (2026-08-14 session, successor coord). Register: `proposed` — steward-floated in dialog, not ratified; his words verbatim below. Routes toward [[dir-disposition]] (template praxes) and the instrumenta segments ( [[norm-affordance-default]], [[form-instrumenta-invocation]] M₀) when adjudicated.*

**The steward's formulation, verbatim (2026-08-14):**

> I'm almost considering a blanket "unless you are purposefully trying to be blind to the surroundings / locus, run aspectus anywhere you go. And if you *are* purposefully blinding yourself, why?"

**The mechanism that makes it strong:** the inversion prices the omission. An un-run glance is currently free and invisible — the priors-shaped-sample failure ("navigate by remembered layout") produces confident wrong findings with nothing to convict them. Making non-observation a decision that must be owned is the same move as absence-claims-carry-their-search, applied to orientation.

**Session evidence (same day):** this session's shallow probe produced a confidently wrong "deployment #2 (paths) vanished" finding; a single `aspectus --lines 300 --depth 5` run falsified it (paths → `references/`, alive and warm) and simultaneously surfaced freeze-supersede/coinage/valve specimens no targeted read had found. Earlier, the steward's `--lines 200 --depth 5` recommendation over asf converted a coarse volume map into per-file resolution in one command. The flag lesson travels with the praxis: default depth under-serves; `--lines 200-300 --depth 5` is the useful register for orientation.

**Theory placement:** aspectus is an M₀ observing instrument ( [[form-instrumenta-invocation]]) — read-only, safely runnable anywhere, no prose constraint needed — and run-on-arrival is the affordance law's *read-side* instance ( [[norm-affordance-default]]): designed perception, per the tracking-snapshots-as-perception bridge.

**The one legitimate blinding case, made precise rather than fuzzy:** de-novo / anti-priming work. Note which half primes: the shape columns (tree, lines, file kinds) are arguably auditor-safe; **heat/age are priming-ish** (they reveal where recent work concentrated — the recency signal a de-novo audit must not inherit). Candidate refinement if this ratifies: the de-novo exemption is "aspectus without heat" (a flag, if it grows one), not "no aspectus."

**Open (for adjudication):** whether this lands as a dir-disposition bullet (estate-general), an instrumenta segment note, or both; and whether the heat-suppression flag is worth requesting from the aspectus tool itself.
