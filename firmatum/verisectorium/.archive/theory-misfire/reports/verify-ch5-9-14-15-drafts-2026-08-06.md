# Verify pass — ch. 5/9/14/15 drafts (2026-08-06)

Adversarial read of eleven segments drafted 2026-08-05/06: `experiential-reading`, `role-activation`, `orientation-scaffold`, `work-coordination`, `dag-vs-exposition`, `plain-language-briefs`, `corpus-instruments`, `instrument-failure-census`, `hotness-methodology`, `observation-stores`, `steward-surfaces`, `authority-routing`. Method: read each segment against the ONTOLOGY norms, then re-read the specific live sources it claims first-hand, checking quotes, numbers, and provenance-register claims rather than plausibility.

## Bottom line

The batch is in good shape. Every quoted phrase I checked is verbatim or a close, honestly-flagged paraphrase of its source. The provenance-register discipline (first-hand / live-source / inherited-at-a-stated-remove) is applied correctly and legibly in all eleven — no segment claims first-hand where it should say inherited. One numeric count is wrong (below) and needs a fix before this ships as "re-read first-hand." No overclaiming, no appendix-grade material smuggled past the tripwire, no restated-not-complementary neighbours.

## This is wrong — fix before reporting further

**`instrument-failure-census.md`, Specimen 1 table: "`old-*` exemption sites in `bin/lint-outline` | 3 | 2026-08-06, re-read first-hand."**

I re-ran this myself. `bin/lint-outline` has exactly **two** places that check `slug.startswith("old-")` — line 236 and line 260. Line 250 is a docstring comment (*"Flags, per OUTLINE row (old-\* segments exempt per FORMAT)"*) describing the policy, not a third exemption check. I could not find a third site under any other spelling (`"old_"`, regex, a constant). Either the count should be 2, or the segment means something looser than "sites in the source that exempt the class" and should say so — as written, the table reads as a mechanical count and isn't one. This is a small fix (one table cell) but it's exactly the kind of number this segment exists to make trustworthy, and `corpus-instruments.md` cites this specimen as its verified-first-hand grounding, so the error would propagate.

Everything else in that specimen checked out: 29 live segments / 43 `old-*` files in `02-tst-core/src/` (72 total) — exact match on both counts, re-run myself.

## Per-segment verdicts

**`experiential-reading`** — Clean. The de-novo SOP's framing quote ("explicitly watching how cognition changes...") and the §7.15 "incidental gold" material match verbatim. The strength register ("no comparison has been run... single estate, single protocol") is honest and matches what the source actually claims (an experiment, not a validated result). No issue.

**`role-activation`** — Clean, and the strongest-sourced of the batch. The §4.4 "Aside" incident (agent batched 5 segments, "the 'consider' never fired as a decision point") matches the source almost word for word — I read the full aside (de-novo.sop.md lines 299-309) and the segment's compression preserves the mechanism accurately, including the corrective ("multi-path read call is itself the decision point"). Correctly labeled as one self-reported anecdote, not a general finding.

**`orientation-scaffold`** — Clean. Every specific claim about `vivarium/ORIENT.md` checks against the live file: the 5/5 vs 3-4/5 vs <3 grade table, the soft-reject-doesn't-burn distinction, the single-sealed-test-per-checkout parallel-clobber hazard, the starred-rows-drive-the-quiz coupling, the owned emergency bypass (`bin/orient-unseal` / Joseph's-approval-to-bypass). "No measurement exists that oriented sessions produce better work" is an honest, correctly-scoped disclaimer — nothing in ORIENT.md claims otherwise.

**`work-coordination`** — Clean. §3.1 ("Delegation of comprehension") and §7.11 (multi-pass, "even if the orchestrating agent already has that context") both check verbatim against the source. The segment is explicit that its reconciliation of §3.1 vs §7.11 ("the seam, not the delegation, fails") is its own synthesis, not the source's statement — correctly marked as inherited-annotator-reading rather than first-hand-source-claim. Good discipline.

**`dag-vs-exposition`** — Clean, and it correctly declines to restate `dependency-order-tension` — I read both side by side. `dependency-order-tension` is about the *checkable* half (a linter validating declared order against the DAG). `dag-vs-exposition` is explicitly about the *un-checkable* residual — exposition failures no `depends:` declaration captures — and says so in its own first paragraph. Not a duplicate; a genuine complement. The §§4.2-4.3 quotes ("silently jumping forward defeats the audit," "often a finding waiting to happen") match the source exactly.

**`plain-language-briefs`** — Clean, and correctly distinct from `pedagogy-layering` (I read both). `pedagogy-layering` governs framing *prose* (introductions, preambles) applied at an author's discretion; `plain-language-briefs` proposes making the same discipline a *per-record field* with a pass/fail test. The segment states this distinction itself rather than leaving it implicit. The Feynman-criterion quotes from `agents.sop.md` ("a thoughtful non-specialist can re-derive the qualitative claim... without seeing the symbols," "most segments do not yet meet it," "often produces the Brief last") are all verbatim matches against the live file. The overclaiming-for-concision risk is correctly marked "inherited at a further remove" rather than promoted to first-hand.

**`corpus-instruments`** — Clean in its own right, but its Specimen-1 grounding inherits the miscount above from `instrument-failure-census` — see that fix. The declared-blindness / worthless-signal / mis-scoped-rule taxonomy is honestly scoped ("four specimens... no frequency claim is available or attempted").

**`instrument-failure-census`** — See the wrong-count finding above (Specimen 1). Specimens 2 and 3 are correctly marked "inherited at a stated remove" with an explicit staleness caveat (2026-07-07 counts, known stale, not re-run) — good discipline, exactly matching `|segment[provenance-registers]`'s third register. Specimen 4 (the `bin/provenance` fabrication) I re-verified against `vivarium/CLAUDE.md` directly: "thirteen hashes... twelve... every one copied," the thirteenth resolving to zero roots, and the tool's own first-run misreading the world's name as "freely" from the manifest comment "rename freely" — all verbatim-accurate.

**`hotness-methodology`** — Clean, and unusually well-verified. I read `vivarium/bin/orient-rank` directly: the "deliberately imperfect... good enough to move with confidence; rebalance when we have evidence" quote is exact. The six factors (freshness, path churn, slug mentions, transitive dependents, working-notes residue, mention-graph PageRank) match the source's own list exactly, including the geometric-mean-for-bottleneck-behavior design choice and the post-mean type prior and tip-fiat. "Nothing has measured whether this ranking picks better records than any other ordering, including alphabetical" is an honest, unhedged admission the source itself does not make explicitly but that follows correctly from what the source does claim (a first-guess weighting, explicitly unvalidated).

**`observation-stores`** — Clean. I re-checked `relata/README.md` directly: "mutable status fields lose that history silently" is verbatim; "the store... was empty until 2026-07-10" is verbatim; the labelled-decision / verification-event / fetch-attempt store trio and the per-actor-timestamped-filename non-collision property all check out. The segment is honest that "the store lives in an external data tree, so its current fill was not inspected here" — correctly scoped, doesn't overclaim currency.

**`steward-surfaces`** — Mostly clean; two of its four source claims are explicitly marked "neither was re-read here" (the comproprium README and the neurips processing-flow document) — I did not re-verify those either, per the segment's own honest flag, and neither did the segment claim to. The parts I could check — the ASF altitude ladder and per-file audience declarations in `agents.sop.md`'s File Organization section (TODO.md "priming-heavy — auditor-hidden," JOSEPH-TODO.md "tracker-class — auditor-hidden," etc.) — match exactly.

**`authority-routing`** — Clean. `JOSEPH-TODO.md`'s admission-test language ("irreversible, publication/authoring-voice, cross-project blast-radius, did this actually come from Joseph... otherwise default-and-proceed") matches the segment's four criteria precisely, and the one-sitting-brief-set pointer (`msc/decision-briefs-2026-07-15.md`) is real and referenced correctly by the source. `vivarium/core/src/norm-decision-authority.md`'s council-accepted tier, the "never upgrades the who-decided field" rule, and the founding incident (grid verdict closed without adjudication) all match verbatim. The `relata` "a parked document is a question the system is asking" quote is exact.

## Cross-cutting

- **Strength-section honesty.** The "no measurement exists" disclaimers throughout (`experiential-reading`, `dag-vs-exposition`, `orientation-scaffold`, `hotness-methodology`, `plain-language-briefs`) read as genuine scope statements, not alibis — each names specifically what *hasn't* been tested and, in several cases, what the cheap test would be. I didn't find one that was doing rhetorical work to excuse an actual overclaim elsewhere in the body.
- **Provenance-register discipline** (`|segment[provenance-registers]`) is applied correctly across all eleven: first-hand claims are things I could verify by re-reading the named file myself; "inherited at a stated remove" claims are consistently the ones the segment did *not* re-verify, and each says so explicitly rather than letting the remove silently collapse.
- **Readable-claims tripwire.** No table or count-of-more-than-two appears inside any of the ten `emp`/`form` segments — the one table in the batch (`instrument-failure-census`, an `obs`) is correctly appendix-placed.
- **No restated neighbours.** Checked `plain-language-briefs` vs `pedagogy-layering` and `dag-vs-exposition` vs `dependency-order-tension` + `appendix-placement` directly; both pairs are genuinely complementary, and each new segment states its own boundary against its neighbour in its own first paragraph rather than leaving the reader to infer it.

## Would have said differently (not wrong, just a note)

- `instrument-failure-census`'s Method & scope section says specimens 1 and 4 were "verified first-hand 2026-08-06," which is true of the file-reading but, given the count error, I'd want the fix and a re-run before trusting the "first-hand" label on Specimen 1's numbers specifically — the read was first-hand, the count wasn't accurate.
- Nothing else struck me as a register or framing choice worth flagging as "I'd have done it differently."

I'm available for follow-up if useful.
