; PRACTICA — strategic navigator for NAME-ME (this instance)
; Top of the intention DAG + immediate WIP. Deep material lives in the outline,
; def/, src/, and influx/; this file stays thin and current.
; Update :updated + area states at every session boundary.
; Read ORIENT.md before working any area.

|practica[NAME-ME] :updated <YYYY-MM-DD>

|area[founding] :priority primary :state active :opened <YYYY-MM-DD>
  :why Freshly deployed from the verisectorium template; the instance is not
       itself until its surfaces are named and seeded.
  :next Rename NAME-ME everywhere (CLAUDE.md title + what-this-is paragraph,
        the outline file, this practica). Write the founding CHANGELOG entry
        (history layer is a required birth-store). Seed the outline's Part I
        terms; draft [[dir-domain-disposition]] and the first def/ entries;
        make ORIENT.md (dir-orient) yours.
