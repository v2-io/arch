# CHANGELOG — history layer (newest first)

*What happened and why. Present truth lives in theory/ (segments + OUTLINE); process rules in theory/sop/ORIENT.md; state in PRACTICA.ud. The misfire-era CHANGELOG.un is archived beside the rest of that attempt's record.*

## 2026-08-14 (third successor session, continued) — rc1 landed whole; audited; folded upward

- **All chartered rc1 files landed** (01-SPINE · 02-RECORD-OBJECT-MODEL · 03-DIMENSIONS · 04-CYCLE · 06-EDGES · 07-DISPOSITION-AND-GAP-ECONOMICS · 08-CERTIFICATION-AND-AUDIT · 09-LANGUAGE sketch · 90-PROVENANCE-AND-FORKS · SPECIMEN), all `proposed`. Steward corrections landed live throughout: "epistemic dimensions" elevated (coord-coined in passing, steward-noticed); dimension re-grained to the orthogonality unit with the unrolled-permutation fusion test; FAMILY reduced to a namespace in the name (one-commit object lifespan); Disposition rehabilitated as placement-for-influence; completeness claim denied by construction; promise-frame deleted from the spine after a letter-vs-intent catch; **under-recording corrected to full severity** (as dishonest as an unwarranted lift; poisons absence-semantics — decline-events added as the semantic remedy).
- **De-novo read run and landed verbatim** (`influx/rc1-denovo-read-2026-08-14.md`): an unprimed stranger reconstructed the model from the set alone; its five strain points all worked same-day (SPECIMEN; the landing seam; stranger glosses; trusted base + under-recording assigned to and answered in 08). First launch stopped for a taxonomy-primed brief — the two-shot law lived; praxis 10 banked with the steward's ASF-genesis testimony (bare briefs generative; freed auditors converge on honesty).
- **Two insights folded into the stopgap system prompt** (steward-invited; PROVENANCE-mapped): §V "The bare brief is generative"; §III interrupt 8's ghost-second-act + the gloss deletion-test.
- Cross-architecture read (grok, headless, same bare brief) launched; lands in influx when complete.

## 2026-08-14 (third successor session, Fable, fresh context) — segments-model-rc1 chartered

- **`theory/influx/segments-model-rc1/00-OUTLINE.md`** (proposed): consolidation surface chartered at the steward's direction after the second session closed near context-end — three generations of partial models (taxonomy arc / 01-MODEL currencies / nets+06-COUPLING) named as one object under three frames, with formalism capture (net-as-spine) the hazard to repair. Nine content files proposed in dependency order; the one-paragraph model statement carried at top; deliberate exclusions declared (no burst-folding while the failure-mode series is open, no epistemology decision, no v4 net, no coinage). Content lands incrementally beside it.
- Orientation finding banked in-dialog: the external-ontologies survey's laws (L1–L8) have substantially landed in 01-MODEL, but **typed epistemic edges** and **gap-economics (EVI/decision-inflection/Walker vocabulary)** have landed nowhere — proposed as RC1 files 06/07; the era-registry gap (era-keys with no registry) proposed in 05; the VERA one-node-type round possibly dissolved by kind-as-currency-profile (routed to the forks file).

## 2026-08-14 (second successor session, Fable) — the truthification cycle modeled executable

- **`theory/influx/model-2026-08-14/net/`**: the steward's truthification-cycle sketch (observe → ideate → concurrent truth-lines → results → standing ledger → decide/teach/apply, looped) captured verbatim (`steward-truthification-sketch-…verbatim.md`, frozen, five bursts incl. the lit-line correction) and modeled as an **executable colored Petri net** (SNAKES + graphviz: `truthification-net.py` / `.dot` / `.svg` / `.png` / `RUN-LOG.txt`, modeling notes + open questions in `NOTES.md`, register `proposed`). The formalism resolution: CPN as base (tokens = records, color = state, generative transitions), the causal DAG as the net's unfolding, and Kahn determinacy kept as a design *law* on pay-in (bag semantics ⇒ arrival-order-invariance, verified by running both orders). Ladders fleshed per line as guard vocabularies + rung orderings + ceiling laws from estate sources; steward-caught mid-modeling: **transmission is a wrapper family, never lock-independent of its source's method-family** — the lock looks through the wrapper (counter-demo in the run), and the lit line gains its early-feed and scope-influence roles. Run also demonstrates certificate decay (era bump expires sim@k1, visibly, minting the re-run question) and re-verification (walls move: primary fetch re-arms the lock through an independent family).
- Session dialog also produced (in-chat, banked here until routed): the max/gcd/lock three-projection reading of aggregation with its GUM-commensurability analysis; the line-as-first-class-object proposal (Lakatos as the unused surveyed precedent for line-health-over-time); and the coord's own thrice-caught superlative pattern (steward-calibrated; tell identified as praise-while-summarizing).

## 2026-08-14 (successor session, Fable) — ingest-first distillation lands

- **`theory/influx/model-2026-08-14/`** (pristine dir, steward-invited): the epistemology/segment-feature model, formed after a full-corpus ingest (accounting from tool log; whole-read: all segment-research non-quarantined + all ten sweeps + REASSEMBLED-MODEL + all 35 segments + brainstorms + trio primary + both ennaos primaries + terminology survey + live ground truth in asf/vivarium/udon-theory/comproprium/references(né paths)/AISI). 00-README (register + sequence honesty) · 01-MODEL (adjudication currencies + the eight-question kind derivation + universal machinery + certification + inculcation loop) · 02-GROUNDING (per-element carried/synthesized/open; five steward forks F1–F5) · 03-ADVERSARIAL-READ (quarantined four opened only after 7489dcd; amendments A1–A4 adopted, A5–A6 noted; selective-unquarantine of asf-implications' observational half routed to steward). All `proposed`.
- **sop/influx**: `aspectus-on-arrival-2026-08-14.md` — steward-floated praxis candidate (run aspectus anywhere you go; purposeful blindness must state its why), verbatim, with same-day falsified-finding evidence and the de-novo heat-priming precision.
- Stale-pointer finds banked in the model dir + session record: udon/v2 `paths/` → `references/` rename left ORIENT/influx pointers stale (rot is rename-shaped, not loss); cross-member ref dialect divergence still open; AISI outline exercises scoped/per-clause Max values the taxonomy has not formalized.

## 2026-08-10 — taxonomy day: survey → skeleton → fill → external grounding (steward + coord)

- **Segment-kind survey run and corrected.** Four sonnet dives (asf lineage / comproprium-kin / udon trees / papers+roots) + coord primary browses landed in `influx/segment-research/`; synthesis-v0 (11 families, 18 axes). Two method defects caught by the steward and banked as sop-influx feedback: the survey produced kind-*lists* titled "distribution" with no counts (the census, run after the catch, showed `discussion` as asf vol-1's most common type), and — the root — *the survey was allowed to bound the generative space*: absence-in-the-wild of an adjudication instrument was read as absence-of-category, deleting Exposition & Pedagogy (the majority family) until caught. Directional praxis now on record: design→survey is completeness-checking; survey→design is the sin.
- **"Generative law" retracted → kind-individuation forcing function.** The coord's "kinds distinct iff failure mode + adjudication instrument differ" was challenged by the steward as hypothesis-dressed-as-imperative: the "iff" was an unsourced strengthening of two same-estate design conventions. Re-registered at hypothesis tier, then redeemed under the steward's grip: per candidate kind, *force* "what SHOULD fail / what SHOULD adjudicate?" and hold the kind open until answered. Traveling rule: a gap declaration needs no argument; an exclusion that argues is a gap in costume.
- **TAXONOMY-DRAFT (steward-shaped skeleton: guiding context + committed table shapes) + FILL-v1 (coord want-mode fill: 11 families incl. restored Exposition with Claim License + referent-coupled Freshness as its yields; Authority ladder with rejected/superseded terminals; Efficacy exemplar; FM?/Instr? working-map columns whose ~ cells are the discussion queue; ∅-instrument conjecture).** Round plan + first-pass leverage ranking in PRACTICA (Exposition → Definitions → Norms → Questions → …; Evidence steward-paced).
- **External-grounding deep research launched** (steward-directed): nine sonnet search→deepen lanes over established ontologies (GRADE/Admiralty/GUM+Hubbard/TRL/IETF-W3C/speech-acts+deontic/PROV+source-criticism/Bloom-SOLO-IRT/NUSAP+second-order uncertainty) → `influx/segment-research/external-ontologies/`. Three midcourse steward corrections: gather-only (synthesis reserved to the coord in-context — opus/sonnet confabulate synthesis), depth unbound for gatherers, "write for truth, not for us."
- Also: authority register ratified as a third axis (decided-by projected); `supported` joined it; the epistemic map's registers/ladders synthesis drawn and twice corrected by dive evidence; glance-vs-extract + delegation-calibration praxes banked in the sop influx from steward observations.

## 2026-08-09 (evening) — instance front door + full orientation + template repair (steward + coord)

- **The theory instance got its own CLAUDE.md front door** (adapted from the template's; AGENTS.md symlinked), after a fresh session earlier in the day misfired against the under-provisioned root. ORIENT elaborated to the upgraded template's structure (contextual-primaries table, named-hazards surface — including the misfire as the under-provisioned-front-door hazard) and its doctrina made whole-corpus: the full drafted-23 listing with grep freshness check, the pre-drafted stubs, template sop store, and influx surfaces as ls-and-know (steward correction: item reads are PRACTICA-dictated, not wholesale).
- **Full orientation exercised end-to-end by the coord** (every `src/*` and `sop/**` in both stores, experiential mode) — the first complete run of the store's own doctrina sequence; gaps it surfaced were fixed in ORIENT as they were found.
- **Template repair pass:** the two outline rows asserting files that didn't exist got their files (`ref-hazards` seed with admission test; `ref-verisectorium-tools` with an explicit built-vs-planned split); `influx/` gained its missing manifest (`00-INDEX.md` — the membrane's entry point with typed-outcome vocabulary and delete-test semantics); `dir-orient` inherited the day's three orientation lessons (route to the sop outline + enums; stubs join the read; influx ls-and-know). Held for the steward: the --GAP-- epistemology row, the ratified/ruled status-vs-state question, the standing enum review.

- **`template/` cut to a blank-slate deployable instance.** The steward had copied the udon paths instance in as raw material and re-cut `sop/SOP.outline.md`; this session stripped the paths content (defs.source.ud, the addressing outline, ref/ primaries + papers, influx payloads — all verified preserved in the paths tree before deletion; `theory-and-lexicon.md`'s integration banner exists only in paths-less form there, flagged) and genericized the rest: `CLAUDE.md` (NAME-ME front door + layout table), `NAME-ME.outline.md`, DECISIONS/PRACTICA/CHANGELOG/LEXICON skeletons, empty `ref/`. Orientation atomized: `sop/src/dir-orient.md` (override; ORIENT.md's new symlink target) + `sop/src/dir-disposition.md` (template-maintained general praxes); paths' monolithic disc-orient retired from the template.
- **New conventions exercised in the template's SOP outline (steward-designed):** the process-state axis gains kit states — `override` (seeded, then deployment-owned; exemplar + bootstrap) / `template` (upstream-owned; upstream change temporarily un-ratifies) / `proposed` (deployment's to draft) — one MECE axis with drafted-and-beyond. Directive epistemic register held as `proposed / ratified / ruled` with `current` as decaying freshness; **open question recorded:** whether ratified/ruled are epistemic status or process state. Outline columns gain `Epistemic Status`; column semantics articulated (all columns are frontmatter denorms except the Designator = filename and the Description = the view's own *perspective*) and landed in [[claim-outline-as-view]].
- **influx lowercased estate-wide** (steward call, reversing the earlier caps lean): influx is not a tracking file and should not outshine `src/`/`sop/`/`def/`. `theory/INFLUX` → `theory/influx`, `theory/sop/INFLUX` → `theory/sop/influx`, live-surface references updated (history layer left as written).
- **`term` noun retired** in favor of def/definition/lexicon: `template/TERM.term.un` moved to `theory/influx/` for adjudication alongside the terminology-survey report; the paths instance's live `def/` entries noted as the best current evidence for the recommended form.

## 2026-08-07 — realization day (founding coord + steward)

- **Deployment #2 founded:** `firmatum/udon/v2/paths/` set up as a full verisectorium (outline + 8 segments split from its theory doc, 26 terms, ORIENT + CLAUDE wiring, INFLUX, CHANGELOG, PRACTICA) and handed to the udon coord. Its founding fed two kit corrections back the same day: the **history layer is required from birth** (scattered meta-crumbs are the O16 record-instinct with no home — this very file exists by that rule), and **host-vs-kit tracking dialect** is a launch-questionnaire item.
- **Part IV (Deployment Components)** added to the theory outline: 19 rows, five chapters — the realization stratum. SOP store founded (`theory/sop/ORIENT.md`); PRACTICA replaced HANDOFF (archived).
- **Orientation vocabulary settled:** doctrina / praxes / **professio** (sponsio retired as too binding for young minds; assertio reserved by the steward). Instance naming/paths brainstorm captured; six partial specs of one addressing scheme recognized → fed to the udon paths work as a working instance rather than a memo.
- **Misfire feedback integrated** (5 provisional rows, 14 drafting cautions); DISCUSSION-THOUGHTS O1–O19 cross-mapped (form-crumb-routing, expand-contract aliases, deliberateness hazard); claim-dispatch-compounds landed with its bifurcation dynamics.

## 2026-08-06 — refounding day (founding coord + steward)

- Theory refounded whole at `theory/` after the prior attempt was set aside to `.archive/theory-misfire/` (standing avoidance advisory in its README). Outline: nine organs × process dual; conventions set by example and exercised under fire same-day (State-not-Stage, proposed, Expected Type, Max ceilings, wikilinks, form-kind-only slug prefixes, flags-not-gates, type-honesty register). Fourteen segments drafted (four by the coord, ten by forks). INFLUX grew the asf SOPs, the instrumenta gather (REGISTER-RULING governs its labels), phanero, and the steward brainstorms; realization-model-v1 is the live iteration surface. Orientation-depth evidence specimen recorded (misfire vs refounding as near-controlled comparison).

## 2026-08-14 (coord session, Fable)
- Epistemology/segment-features session: coord produced four synthesis files in `theory/influx/segment-research/` (foundation-axis, distilled-model, asf-implications, securing-contract) — then Joseph caught the root failure: the frame gelled after ~7 of 40+ relevant primaries, and all later reading assimilated into it as diffs. All four bannered as premature-synthesis testimony, withheld-by-design from de-novo passes.
- Steward's March-2026 epistemic-foundation-scale sketch landed as a primary (`sketch-2026-03-epistemic-foundation-scale.jpeg`).
- `NOTE-TO-SUCCESSOR-2026-08-14.md` written at Joseph's direction: the model-of-record work goes to a fresh agent doing ingest-everything-first-then-distill. Commit a9e5450.
- Lesson for the sop-influx (banked here until the successor or steward routes it): deciding-ready-before-orienting is the root; the felt click of coherence is the danger moment; accounting tables from tool logs, not felt-state.
