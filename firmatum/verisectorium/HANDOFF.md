# HANDOFF — session-boundary notes

*For the next mind picking this up. Newest entry first; retire entries once their content is absorbed into PRACTICA / plan segments / CHANGELOG. This is a working surface, not history — history is [`CHANGELOG.un`](CHANGELOG.un).*

---

## Template

> **date · who → whom (if known)** Where things actually stand (not where they optimistically stand) · what is mid-flight · the next most useful act · anything only this session knew that isn't yet durable elsewhere.

---

## 2026-08-06 (restart session) · claude (fable) + joseph → next session

**The theory was refounded.** The prior attempt was set aside whole to `.archive/theory-misfire/` — read that directory's README before touching it; the standing recommendation is *don't*, while the foundation is being laid. The new work is `theory/`: OUTLINE (nine organs × process dual, ~65 rows with Expected Type / Max / State columns), four drafted segments (`post-living-collection`, `def-verisectorium`, `form-state-flags-not-gates`, `claim-outline-as-view`), and a heavily-grown INFLUX (asf's 13 SOPs; the udon-needs instrumenta gather — its `REGISTER-RULING.md` governs label inheritance; steward brainstorms with the realization model at `steward-brainstorms/realization-model-v1.md` as the live iteration surface).

**Where things stand:** three commits landed on main (scaffold / outline+drafts / brainstorm baseline) — note the branch story: `verisectorium-restart` was created but HEAD returned to main *before* any commits, so main carries everything and the branch is vestigial at `1dd18fd`. Uncommitted at entry-writing: the instrumenta gather (24 files, awaiting joint review), REGISTER-RULING, realization-model-v1, README/HANDOFF/archive-README updates, OUTLINE register edits.

**Load-bearing conventions set this session** (all recorded with lineage in `theory/OUTLINE.md` Working Notes — read those first): State-not-Stage; proposed-not-missing; Expected Type; Max ceiling column; `[[wikilink]]` refs (rendering-form decision open); form-kind-only slug prefixes with `claim-` trajectory-neutral (Joseph reservation on `claim-` recorded — revisit); flags-not-gates; the type-honesty register (source confidence ≠ source evidence — applies with extra force to the instrumenta gather).

**Mid-flight / waiting:** Joseph's interrupted brainstorm continuation ("There also may be…" — slot open in the verbatim baseline); exemplars' home and telos/ethos placement undecided; the wikilink-form and `claim-`-prefix decisions parked; "instrumenta" name collision with an autopax component family flagged for an Organ II check; PRACTICA.un / CHANGELOG.un predate the restart and await refresh or archival.
