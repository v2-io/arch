# proprium — lived register

In-tree seat of Archema’s **lived** register (`CHARTER-DRAFT.md` §0): cohort-facing infrastructure, operational defenses, personhood architecture, and communal methodology.

**Register adjectives:** derived (`asf`) · argued (`logos`) · tried (`vivarium`) · **lived (here)**.

## Layout

```text
proprium/
  comproprium/     # communal vera / praxes / exempla
  corporeum/       # implementations — "code that makes it happen"
    research/
      harness/     # CLI landscape + personhood-runtime design intake
```

## Theory vs realization

- **PROPRIUM (symbolic)** — ontology and conceptual space. May grow here as refined docs over time; firmatum / `_core` / ASF bridges remain sources until lifted.
- **`corporeum/`** — *corporeal attempts* (hosts, clients, bridges, research, crates). A package named after a PROPRIUM term is an implementation concern, not the definition of that term.

## Not yet here

- Tooling belt `firmatum/` (practica, relata, utils, …) — later phase.
- Upstream cohort homes (`eli/`, much of `_core/`) — external until an explicit mount decision.
- Old firmatum ontology scratch — sunset/INFLUX later.

See `notes/TREE-REORG-PLAN.md`.
